diff --git a/node_modules/@native-sdk/cli/src/canopy_terminal_tests.zig b/node_modules/@native-sdk/cli/src/canopy_terminal_tests.zig
new file mode 100644
index 0000000..16cc72d
--- /dev/null
+++ b/node_modules/@native-sdk/cli/src/canopy_terminal_tests.zig
@@ -0,0 +1,4 @@
+//! Consumer test root: unlike the SDK-wide suite, this uses real Ghostty VT.
+test {
+    _ = @import("runtime/terminal_session_tests.zig");
+}
diff --git a/node_modules/@native-sdk/cli/src/platform/macos/appkit_host.m b/node_modules/@native-sdk/cli/src/platform/macos/appkit_host.m
index 3225c78..f11db33 100644
--- a/node_modules/@native-sdk/cli/src/platform/macos/appkit_host.m
+++ b/node_modules/@native-sdk/cli/src/platform/macos/appkit_host.m
@@ -6995,6 +6995,21 @@ - (void)setScrollDrivers:(const native_sdk_appkit_scroll_driver_t *)drivers coun
         driver.occluderMask = desired.occluder_mask;
         NSRect target = NSMakeRect(desired.x, self.bounds.size.height - desired.y - desired.height, desired.width, desired.height);
         if (!NSEqualRects(driver.frame, target)) driver.frame = target;
+        // Native overlay scrollers are AppKit subviews above the Metal canvas.
+        // A full-view modal therefore cannot visually cover or hit-block one
+        // by paint order alone. Hide a driver whenever one of the occluders in
+        // its mask fully covers its frame; nested modal scroll regions carry
+        // no mask and remain visible and interactive.
+        BOOL fullyOccluded = NO;
+        for (NSUInteger occluderIndex = 0; occluderIndex < occluderRects.count && occluderIndex < 32; occluderIndex += 1) {
+            const uint32_t bit = (uint32_t)1 << occluderIndex;
+            if ((driver.occluderMask & bit) == 0) continue;
+            if (NSContainsRect([occluderRects[occluderIndex] rectValue], target)) {
+                fullyOccluded = YES;
+                break;
+            }
+        }
+        driver.hidden = fullyOccluded;
         NSSize contentSize = NSMakeSize(MAX(desired.content_width, 1), MAX(desired.content_height, 1));
         if (driver.documentView && !NSEqualSizes(driver.documentView.frame.size, contentSize)) {
             [driver.documentView setFrameSize:contentSize];
diff --git a/node_modules/@native-sdk/cli/src/primitives/canvas/terminal_grid.zig b/node_modules/@native-sdk/cli/src/primitives/canvas/terminal_grid.zig
index f36fa7d..1956e36 100644
--- a/node_modules/@native-sdk/cli/src/primitives/canvas/terminal_grid.zig
+++ b/node_modules/@native-sdk/cli/src/primitives/canvas/terminal_grid.zig
@@ -28,14 +28,13 @@ const canvas = @import("root.zig");
 const geometry = @import("geometry");
 const box = @import("terminal_box.zig");

-/// Grid ceilings, derived from the per-view canvas budgets: the glyph
-/// budget bounds how many cells can hold ink at once, and the command
-/// budget bounds per-row style runs. A viewport is clamped to these
-/// before it reaches an emulator, so a huge window degrades to a
-/// bounded grid instead of a budget error.
+/// Representable grid dimensions. Painter resources are charged by runs and
+/// distinct glyphs, not by viewport cell count.
 pub const max_cols: usize = 320;
 pub const max_rows: usize = 96;
-pub const max_cells: usize = 7168;
+// Grid geometry is independent from painter budgets: repeated glyphs and
+// merged runs do not cost one atlas entry/command per terminal cell.
+pub const max_cells: usize = max_cols * max_rows;

 /// Painter budgets when the grid rides the WIDGET tree, where it shares
 /// the per-view stores with every other widget. The command reserve is
@@ -226,13 +225,10 @@ pub fn cellMetrics(tokens: canvas.DesignTokens) TerminalCellMetrics {
 }

 /// Clamp a proposed grid to the canvas budgets: the glyph budget bounds
-/// total cells, so a very wide viewport trades rows for columns to stay
-/// under the cell ceiling instead of overflowing the frame.
+/// each axis independently, so widening a viewport never shortens its height.
 pub fn clampGrid(proposed_cols: usize, proposed_rows: usize) TerminalCellPos {
-    var c = std.math.clamp(proposed_cols, 2, max_cols);
-    var r = std.math.clamp(proposed_rows, 2, max_rows);
-    if (c * r > max_cells) r = @max(2, max_cells / c);
-    if (c * r > max_cells) c = @max(2, max_cells / r);
+    const c = std.math.clamp(proposed_cols, 2, max_cols);
+    const r = std.math.clamp(proposed_rows, 2, max_rows);
     return .{ .x = @intCast(c), .y = @intCast(r) };
 }

@@ -414,6 +410,16 @@ fn rowPathElements(row: TerminalRow) usize {
     return total;
 }

+fn squareCorner(cp: u21) u21 {
+    return switch (cp) {
+        0x256D => 0x250C,
+        0x256E => 0x2510,
+        0x256F => 0x2518,
+        0x2570 => 0x2514,
+        else => cp,
+    };
+}
+
 /// Text bytes ALREADY in the display list — every `draw_text`, whether
 /// its bytes are builder-owned (a chart's labels, this grid's own runs)
 /// or referenced from view-owned memory (an earlier text widget). The
@@ -444,41 +450,47 @@ fn displayListPathElements(list: canvas.DisplayList) usize {
     return total;
 }

-/// An UPPER BOUND on the display-list commands one row emits, assuming
-/// no runs merge (the pathological worst case): a background run per
-/// cell that carries one, plus the cell's ink — eight commands for a
-/// pure-double box joint, two (text run + underline) for a styled
-/// character, none for an empty cell — plus one selection wash. Real
-/// rows merge runs and cost far less; this bound is what the per-row
-/// preflight checks so a row is only started when it can finish whole,
-/// and it is content-accurate (a cheap ASCII row costs ~2/column, not a
-/// flat worst-case-per-column reserve that would starve wide grids).
+/// Command upper bound using the same background/text/box run boundaries as
+/// paint. Charging per cell instead of per run discards otherwise cheap dense
+/// TUI rows even when the display list has plenty of remaining capacity.
 fn rowCommandCost(row: TerminalRow) usize {
-    var total: usize = 1; // the selection wash
+    var total: usize = if (row.selection != null) 1 else 0;
+    var background: ?canvas.Color = null;
+    for (row.cells) |cell| {
+        if (cell.bg) |bg| {
+            if (background == null or !colorEql(background.?, bg)) total += 1;
+        }
+        background = cell.bg;
+    }
+    var text_active = false;
+    var text_fg: canvas.Color = undefined;
+    var text_underline = false;
+    var text_bytes: usize = 0;
+    var previous_cluster = false;
     var i: usize = 0;
     while (i < row.cells.len) : (i += 1) {
         const cell = row.cells[i];
-        if (cell.wide == .spacer) continue;
-        if (cell.bg != null) total += 1;
-        if (cell.cp == 0) continue;
+        if (cell.wide == .spacer or cell.cp == 0) {
+            text_active = false;
+            continue;
+        }
         if (box.isBoxDrawing(cell.cp)) {
+            text_active = false;
             if (box.mergesHorizontally(cell.cp)) {
                 // A horizontally-merged run (a long `─` border) paints
                 // ONE geometry command plus a possible underline, no
                 // matter how wide — mirror the paint loop's merge (same
                 // code point, foreground, and underline) so the estimate
                 // does not charge nine per column for what collapses to
-                // two commands. Backgrounds still count per cell (their
-                // own run pass is separate).
+                // two commands. Background runs were counted separately.
                 var span: usize = 1;
                 while (i + span < row.cells.len) : (span += 1) {
                     const next = row.cells[i + span];
                     if (next.cp != cell.cp or !colorEql(next.fg, cell.fg) or next.underline != cell.underline) break;
-                    if (next.bg != null) total += 1;
                 }
                 // A merged run's worst-case ink: a double piece (═)
                 // paints TWO parallel bars, plus a possible underline.
-                total += 3;
+                total += box.maxCommands(cell.cp) + @as(usize, @intFromBool(cell.underline));
                 i += span - 1;
             } else {
                 // The glyph's own worst case (kept lockstep with the
@@ -486,10 +498,23 @@ fn rowCommandCost(row: TerminalRow) usize {
                 // underline — a row of one-bar pieces costs what it
                 // paints, never a flat joint-worst-case that starves
                 // wide grids.
-                total += box.maxCommands(cell.cp) + 1;
+                total += box.maxCommands(cell.cp) + @as(usize, @intFromBool(cell.underline));
             }
         } else {
-            total += 2; // the text run plus its underline
+            const primary_len = std.unicode.utf8CodepointSequenceLength(cell.cp) catch 1;
+            const cluster = cell.cluster.len > primary_len;
+            if (!text_active or !colorEql(text_fg, cell.fg) or
+                text_underline != cell.underline or cluster or previous_cluster or
+                text_bytes + cell.cluster.len > text_scratch_bytes)
+            {
+                total += 1 + @as(usize, @intFromBool(cell.underline));
+                text_bytes = 0;
+            }
+            text_active = true;
+            text_fg = cell.fg;
+            text_underline = cell.underline;
+            text_bytes += cell.cluster.len;
+            previous_cluster = cluster;
         }
     }
     return total;
@@ -612,12 +637,12 @@ pub fn paint(grid: TerminalGrid, builder: *canvas.Builder, options: TerminalPain
         // grid adds, not what its siblings spent.
         const row_text = rowTextBytes(row);
         if (row_text > 0 and text_total + row_text > text_ceiling) break;
-        // Path-element stop, ATOMIC per row, against the view-global
-        // running total (static sibling paths included): a row of rounded
-        // corners is skipped BEFORE it would cross the per-view
-        // path-element budget; a row adding none paints regardless.
+        // Path budget includes static sibling icons. If rounded corners do
+        // not fit, square them while keeping the entire row's content.
         const row_paths = rowPathElements(row);
-        if (row_paths > 0 and path_total + row_paths > path_ceiling) break;
+        // Decoration must not suppress the entire TUI. Square joins need no
+        // path storage and fit the same command bound as arc + straight.
+        const square_corners = row_paths > 0 and path_total + row_paths > path_ceiling;
         // Glyph-budget stop, same row-atomic shape: stop BEFORE the row
         // whose new DISTINCT code points would cross the atlas proxy —
         // the frame degrades to fewer rows instead of failing whole on
@@ -843,7 +868,8 @@ pub fn paint(grid: TerminalGrid, builder: *canvas.Builder, options: TerminalPain
                 // four-wide stride would collide the ids of adjacent
                 // double cells and fail the retained diff with
                 // DuplicateObjectId.
-                box.paint(builder, ids.at(row_id + 0x6000 + @as(u64, @intCast(x)) * commands_per_cell), rect, box_cp, fg, box_thickness) catch {
+                const painted_cp = if (square_corners) squareCorner(box_cp) else box_cp;
+                box.paint(builder, ids.at(row_id + 0x6000 + @as(u64, @intCast(x)) * commands_per_cell), rect, painted_cp, fg, box_thickness) catch {
                     row_torn = true;
                     break;
                 };
diff --git a/node_modules/@native-sdk/cli/src/primitives/canvas/terminal_grid_tests.zig b/node_modules/@native-sdk/cli/src/primitives/canvas/terminal_grid_tests.zig
index ef7c027..bb271a6 100644
--- a/node_modules/@native-sdk/cli/src/primitives/canvas/terminal_grid_tests.zig
+++ b/node_modules/@native-sdk/cli/src/primitives/canvas/terminal_grid_tests.zig
@@ -750,7 +750,10 @@ test "the widget diff reports paint damage for a changed bound grid" {
     try testing.expect(saw_paint_dirty);
 }

-test "clampGrid trades rows for columns under the cell ceiling" {
+test "clampGrid preserves viewport rows independently of its width" {
+    const viewport = grid_model.clampGrid(178, 51);
+    try testing.expectEqual(@as(u16, 178), viewport.x);
+    try testing.expectEqual(@as(u16, 51), viewport.y);
     const clamped = grid_model.clampGrid(400, 100);
     try testing.expectEqual(@as(u16, grid_model.max_cols), clamped.x);
     try testing.expect(@as(usize, clamped.x) * @as(usize, clamped.y) <= grid_model.max_cells);
@@ -1020,6 +1023,43 @@ test "rounded-corner path elements survive the builder's lifetime" {
     try testing.expect(saw_path);
 }

+test "path exhaustion squares TUI corners instead of hiding terminal content" {
+    const cells = [_]grid_model.TerminalCell{
+        .{ .cp = 0x256D, .fg = white },
+        .{ .cp = 'T', .cluster = "T", .fg = white },
+        .{ .cp = 0x256E, .fg = white },
+    };
+    const rows = [_]grid_model.TerminalRow{.{ .cells = &cells }};
+    var commands: [64]canvas.CanvasCommand = undefined;
+    var builder = try paintInto(baseGrid(&rows), &commands, .{
+        .frame = geometry.RectF.init(0, 0, 100, 40),
+        .path_reserve = std.math.maxInt(usize),
+        .tokens = .{},
+    });
+    var saw_text = false;
+    for (builder.displayList().commands) |command| {
+        if (command == .draw_text and std.mem.eql(u8, command.draw_text.text, "T")) saw_text = true;
+        try testing.expect(command != .stroke_path);
+    }
+    try testing.expect(saw_text);
+}
+
+test "dense uniform TUI rows charge merged runs rather than every cell" {
+    const cells = [_]grid_model.TerminalCell{.{ .cp = 'A', .cluster = "A", .fg = white, .bg = black }} ** 178;
+    const rows = [_]grid_model.TerminalRow{.{ .cells = &cells }} ** 40;
+    var commands: [256]canvas.CanvasCommand = undefined;
+    var builder = try paintInto(baseGrid(&rows), &commands, .{
+        .frame = geometry.RectF.init(0, 0, 1600, 1200),
+        .tokens = .{},
+        .command_budget = commands.len,
+    });
+    var painted: usize = 0;
+    for (builder.displayList().commands) |command| if (command == .draw_text) {
+        painted += 1;
+    };
+    try testing.expectEqual(@as(usize, 40), painted);
+}
+
 test "adjacent double-cross cells never collide command ids" {
     // ╬ emits up to eight commands; the per-column id stride must be at
     // least eight so neighbors never share an object id.
diff --git a/node_modules/@native-sdk/cli/src/root.zig b/node_modules/@native-sdk/cli/src/root.zig
index 2da935a..84fb786 100644
--- a/node_modules/@native-sdk/cli/src/root.zig
+++ b/node_modules/@native-sdk/cli/src/root.zig
@@ -149,6 +149,10 @@ pub const max_effect_ptys = runtime.max_effect_ptys;
 pub const max_effect_pty_chunk_bytes = runtime.max_effect_pty_chunk_bytes;
 pub const max_effect_pty_write_bytes = runtime.max_effect_pty_write_bytes;
 pub const max_effect_pty_term_bytes = runtime.max_effect_pty_term_bytes;
+pub const PtyEnvEntry = runtime.PtyEnvEntry;
+pub const PtyEnvVar = runtime.PtyEnvVar;
+pub const max_effect_pty_env_entries = runtime.max_effect_pty_env_entries;
+pub const max_effect_pty_env_bytes = runtime.max_effect_pty_env_bytes;
 pub const EffectHostResult = runtime.EffectHostResult;
 pub const HostCallBinding = runtime.HostCallBinding;
 pub const HostCallCompletion = runtime.HostCallCompletion;
diff --git a/node_modules/@native-sdk/cli/src/runtime/core.zig b/node_modules/@native-sdk/cli/src/runtime/core.zig
index e780399..45a7ce4 100644
--- a/node_modules/@native-sdk/cli/src/runtime/core.zig
+++ b/node_modules/@native-sdk/cli/src/runtime/core.zig
@@ -931,7 +931,7 @@ pub const Runtime = struct {
     pub const gpuSurfaceFrame = CanvasFrameMethods.gpuSurfaceFrame;
     pub const setCanvasFrameBudget = CanvasFrameMethods.setCanvasFrameBudget;
     pub const setGpuSurfaceInputLatencyBudget = CanvasFrameMethods.setGpuSurfaceInputLatencyBudget;
-    const requestCanvasFrameForView = CanvasFrameMethods.requestCanvasFrameForView;
+    pub const requestCanvasFrameForView = CanvasFrameMethods.requestCanvasFrameForView;
     const invalidateForCanvasChanges = CanvasFrameMethods.invalidateForCanvasChanges;

     const CanvasImageMethods = runtime_canvas_images.RuntimeCanvasImages(Runtime);
diff --git a/node_modules/@native-sdk/cli/src/runtime/effects.zig b/node_modules/@native-sdk/cli/src/runtime/effects.zig
index ce420b0..26ab2ba 100644
--- a/node_modules/@native-sdk/cli/src/runtime/effects.zig
+++ b/node_modules/@native-sdk/cli/src/runtime/effects.zig
@@ -1102,12 +1102,16 @@ pub const max_effect_channel_pending: usize = 32;
 /// occupancy like a channel, not a run-to-completion worker slot. One
 /// live terminal surface plus a background job or two is the realistic
 /// shape; a fifth spawn is refused loudly (reason `.rejected`).
-pub const max_effect_ptys: usize = 4;
+/// PTY occupancies are dynamically allocated. This nullable export keeps the
+/// public capability discoverable without imposing an application limit.
+pub const max_effect_ptys: ?usize = null;
 /// Longest one delivered pty output record: one Msg payload, one
 /// journal blob. Output arriving between drains coalesces into batches
 /// of at most this size — `cat largefile` journals per-drain batches,
 /// never per-read records.
-pub const max_effect_pty_chunk_bytes: usize = 64 * 1024;
+/// UI-side delivery quantum. The staging ring remains 256 KiB and lossless;
+/// 4 KiB keeps one terminal flood from monopolizing a Debug-frame VT parse.
+pub const max_effect_pty_chunk_bytes: usize = 4 * 1024;
 /// The cross-thread output staging ring per pty. When it fills, the io
 /// thread stops reading the parent end until the loop drains — kernel
 /// back-pressure to the child, a terminal's native flow control. Bytes
@@ -1133,6 +1137,27 @@ pub const max_effect_pty_outbound_bytes: usize = 64 * 1024;
 pub const max_effect_pty_write_records: usize = 256;
 /// Longest TERM value `ptySpawn` accepts.
 pub const max_effect_pty_term_bytes: usize = 32;
+/// Maximum number of caller-provided environment entries a pty spawn may
+/// overlay onto the bound host environment. The host environment itself is
+/// still subject to the transport's own bounds; this cap limits the
+/// app-owned storage retained by each fake/live pty slot.
+pub const max_effect_pty_env_entries: usize = 32;
+/// Maximum combined `name=value` bytes in one pty environment overlay. The
+/// separator is included in the bound, matching the flattened transport
+/// representation. Values are copied into the pty slot and wiped when the
+/// real spawn has consumed them or the slot retires.
+pub const max_effect_pty_env_bytes: usize = 4096;
+
+/// One caller-provided pty environment overlay entry. Names are non-empty,
+/// cannot contain `=`, NUL, or `TERM` (TERM is controlled by `PtySpawnOptions`
+/// `.term`); values may be empty but cannot contain NUL. The bytes are copied
+/// into slot storage when `ptySpawn` accepts the request.
+pub const PtyEnvEntry = struct {
+    name: []const u8,
+    value: []const u8,
+};
+/// Compatibility spelling for callers that describe an entry as a variable.
+pub const PtyEnvVar = PtyEnvEntry;
 /// Bytes of `ptyWrite` input a FAKE pty retains for test assertions
 /// (`ptyWrittenBytes`) — the scriptable pty's inspection window, not a
 /// delivery bound: writes beyond it drop oldest-first, exactly what a
@@ -1204,6 +1229,14 @@ pub const EffectPtyEvent = struct {
 pub const PtyTap = struct {
     context: *anyopaque,
     notify: *const fn (context: *anyopaque, event: *const EffectPtyEvent) void,
+    /// An accepted spawn owns a fresh terminal lifetime even when its key
+    /// recycles an ended transport. Notify the emulator before the child can
+    /// emit output so a newly mounted terminal never paints stale scrollback.
+    spawn: ?*const fn (context: *anyopaque, key: u64) void = null,
+    /// Release framework-owned terminal presentation state after the app has
+    /// removed the corresponding tab. This does not kill a transport; callers
+    /// retire the PTY first and forget its emulator after the exit delivery.
+    forget: ?*const fn (context: *anyopaque, key: u64) void = null,
 };

 /// What one channel event Msg reports. `.data` is one delivered post;
@@ -2255,41 +2288,136 @@ fn fallbackEnviron() std.process.Environ {
     }
 }

-/// Flatten the bound environ into the pty transport's env list — the
-/// spawn effect's inherit-the-host policy, made explicit because execve
-/// takes a flat envp. TERM is injected by the transport whether or not
-/// the host carried one. Returns null when the environ exceeds the
-/// transport's bounds: the child MUST see the whole environment the API
-/// promised, so an over-bound environ fails the spawn loudly rather
-/// than handing the child a silently truncated one (a required variable
-/// past the cutoff would otherwise vanish). On Windows the bound
-/// environ is the live-process sentinel (`GlobalBlock`), so the
-/// transport snapshots the actual environment into `bytes` — same
-/// policy, same loud over-bound refusal, one extra storage hop because
-/// the OS block is WTF-16.
+/// Compare environment names using the host's environment semantics. POSIX
+/// names are byte-sensitive; Windows names are ASCII case-insensitive (the
+/// transport applies the same rule while it builds its environment block).
+fn ptyEnvNameEqual(a: []const u8, b: []const u8) bool {
+    if (comptime builtin.os.tag == .windows) {
+        if (a.len != b.len) return false;
+        for (a, b) |left, right| {
+            if (std.ascii.toUpper(left) != std.ascii.toUpper(right)) return false;
+        }
+        return true;
+    }
+    return std.mem.eql(u8, a, b);
+}
+
+/// Return the flattened `name=value` byte length, including the separator,
+/// or null when the transport's total environment bound cannot represent the
+/// entry without arithmetic overflow.
+fn ptyEnvEntryBytes(name: []const u8, value: []const u8) ?usize {
+    if (name.len >= pty_transport.max_env_bytes) return null;
+    const remaining = pty_transport.max_env_bytes - name.len - 1;
+    if (value.len > remaining) return null;
+    return name.len + 1 + value.len;
+}
+
+/// Validate the caller-owned part of a pty environment before claiming a
+/// slot. This is a regenerating request bound: fake, live, and replay all
+/// refuse the same malformed overlay without depending on the host's
+/// environment or transport availability.
+fn validPtyEnvOverlay(env: []const PtyEnvEntry) bool {
+    if (env.len > max_effect_pty_env_entries) return false;
+    var total_bytes: usize = 0;
+    for (env) |entry| {
+        if (entry.name.len == 0 or
+            std.mem.indexOfAny(u8, entry.name, &.{ '=', 0 }) != null or
+            std.mem.indexOfScalar(u8, entry.value, 0) != null or
+            ptyEnvNameEqual(entry.name, "TERM"))
+        {
+            return false;
+        }
+        if (comptime builtin.os.tag == .windows and
+            (!std.unicode.wtf8ValidateSlice(entry.name) or !std.unicode.wtf8ValidateSlice(entry.value)))
+        {
+            return false;
+        }
+        if (entry.name.len >= max_effect_pty_env_bytes) return false;
+        const remaining = max_effect_pty_env_bytes - entry.name.len - 1;
+        if (entry.value.len > remaining) return false;
+        const entry_bytes = entry.name.len + 1 + entry.value.len;
+        if (total_bytes > max_effect_pty_env_bytes - entry_bytes) return false;
+        total_bytes += entry_bytes;
+    }
+    return true;
+}
+
+/// Flatten the bound environ into the pty transport's env list and merge the
+/// caller's bounded overlay. The overlay replaces an inherited value with
+/// the same name and appends otherwise; TERM is excluded from both sides and
+/// injected by the transport from `PtySpawnOptions.term`. Returns null when
+/// the complete result exceeds the transport's bounds: the child MUST see
+/// the whole environment the API promised, so an over-bound environ fails
+/// the spawn loudly rather than handing the child a silently truncated one.
+/// On Windows the bound environ is the live-process sentinel (`GlobalBlock`),
+/// so the transport snapshots the actual environment into `bytes` — same
+/// policy, same loud over-bound refusal, one extra storage hop because the
+/// OS block is WTF-16.
 fn flattenPtyEnviron(
     environ: std.process.Environ,
     buffer: []pty_transport.EnvVar,
     bytes: []u8,
+    overlay: []const PtyEnvEntry,
 ) ?[]const pty_transport.EnvVar {
-    if (comptime builtin.os.tag == .windows) {
-        if (!environ.block.use_global) return buffer[0..0];
-        return pty_transport.captureGlobalEnviron(buffer, bytes);
-    }
-    if (comptime std.process.Environ.Block != std.process.Environ.PosixBlock) return buffer[0..0];
     var count: usize = 0;
     var total_bytes: usize = 0;
-    for (environ.block.slice) |entry_opt| {
-        const entry = entry_opt orelse continue;
-        const text = std.mem.span(entry);
-        const eq = std.mem.indexOfScalar(u8, text, '=') orelse continue;
-        // The transport injects its own TERM; a host TERM would race it.
-        if (std.mem.eql(u8, text[0..eq], "TERM")) continue;
-        if (count == buffer.len) return null;
-        if (total_bytes + text.len > pty_transport.max_env_bytes) return null;
-        buffer[count] = .{ .name = text[0..eq], .value = text[eq + 1 ..] };
-        count += 1;
-        total_bytes += text.len;
+    if (comptime builtin.os.tag == .windows) {
+        if (environ.block.use_global) {
+            const captured = pty_transport.captureGlobalEnviron(buffer, bytes) orelse return null;
+            count = captured.len;
+            for (captured) |entry| {
+                const entry_bytes = ptyEnvEntryBytes(entry.name, entry.value) orelse return null;
+                if (total_bytes > pty_transport.max_env_bytes - entry_bytes) return null;
+                total_bytes += entry_bytes;
+            }
+        }
+    } else {
+        if (comptime std.process.Environ.Block == std.process.Environ.PosixBlock) {
+            for (environ.block.slice) |entry_opt| {
+                const entry = entry_opt orelse continue;
+                const text = std.mem.span(entry);
+                const eq = std.mem.indexOfScalar(u8, text, '=') orelse continue;
+                // The transport injects its own TERM; a host TERM would race it.
+                if (std.mem.eql(u8, text[0..eq], "TERM")) continue;
+                if (count == buffer.len) return null;
+                const entry_bytes = ptyEnvEntryBytes(text[0..eq], text[eq + 1 ..]) orelse return null;
+                if (total_bytes > pty_transport.max_env_bytes - entry_bytes) return null;
+                buffer[count] = .{ .name = text[0..eq], .value = text[eq + 1 ..] };
+                count += 1;
+                total_bytes += entry_bytes;
+            }
+        }
+    }
+
+    // Apply each caller entry in order. Repeated names intentionally follow
+    // the ordinary overlay rule: the last value wins, while replacing the
+    // existing slot keeps the flat envp free of duplicate names.
+    for (overlay) |entry| {
+        var replaced: ?usize = null;
+        for (buffer[0..count], 0..) |existing, index| {
+            if (ptyEnvNameEqual(existing.name, entry.name)) {
+                replaced = index;
+                break;
+            }
+        }
+        const entry_bytes = ptyEnvEntryBytes(entry.name, entry.value) orelse return null;
+        if (replaced) |index| {
+            const old_bytes = ptyEnvEntryBytes(buffer[index].name, buffer[index].value) orelse return null;
+            if (entry_bytes >= old_bytes) {
+                const delta = entry_bytes - old_bytes;
+                if (total_bytes > pty_transport.max_env_bytes - delta) return null;
+                total_bytes += delta;
+            } else {
+                total_bytes -= old_bytes - entry_bytes;
+            }
+            buffer[index] = .{ .name = entry.name, .value = entry.value };
+        } else {
+            if (count == buffer.len) return null;
+            if (total_bytes > pty_transport.max_env_bytes - entry_bytes) return null;
+            buffer[count] = .{ .name = entry.name, .value = entry.value };
+            count += 1;
+            total_bytes += entry_bytes;
+        }
     }
     return buffer[0..count];
 }
@@ -2453,8 +2581,6 @@ fn ptyStageOutput(shared: *PtyShared, generation: u64, bytes: []const u8) void {
                     _ = owner.pending.fetchAdd(1, .seq_cst);
                     stamped = true;
                 }
-            } else if (take > 0) {
-                stamped = true;
             }
         }
     }
@@ -2544,6 +2670,256 @@ fn ptyFlushOutbound(shared: *PtyShared, transport: pty_transport.Pty, generation
     }
 }

+/// One stable registration in the Darwin kqueue reactor. Registrations are
+/// retained until reactor teardown so a queued kernel event can never point at
+/// freed memory; `active` and `generation` make stale events inert.
+const PtyReactorEntry = struct {
+    // Registration publishes the entry to kqueue before it becomes active.
+    // An immediately-ready fd may therefore produce a harmless ignored event,
+    // while a partially failed registration can never be serviced.
+    active: std.atomic.Value(bool) = std.atomic.Value(bool).init(false),
+    reclaim_after: std.atomic.Value(u64) = std.atomic.Value(u64).init(0),
+    shared: *PtyShared,
+    transport: pty_transport.Pty,
+    generation: u64,
+};
+
+const PtyReactor = struct {
+    allocator: std.mem.Allocator,
+    platform: pty_transport.Reactor,
+    mutex: SpinMutex = .{},
+    entries: std.ArrayListUnmanaged(*PtyReactorEntry) = .empty,
+    shutdown: std.atomic.Value(bool) = std.atomic.Value(bool).init(false),
+    epoch: std.atomic.Value(u64) = std.atomic.Value(u64).init(1),
+    thread: std.Thread,
+
+    fn create(allocator: std.mem.Allocator) ?*PtyReactor {
+        if (comptime !pty_transport.reactor_supported) return null;
+        const reactor = allocator.create(PtyReactor) catch return null;
+        const reactor_platform = pty_transport.Reactor.init() catch {
+            allocator.destroy(reactor);
+            return null;
+        };
+        reactor.* = .{
+            .allocator = allocator,
+            .platform = reactor_platform,
+            .thread = undefined,
+        };
+        reactor.thread = std.Thread.spawn(.{}, run, .{reactor}) catch {
+            reactor.platform.deinit();
+            allocator.destroy(reactor);
+            return null;
+        };
+        return reactor;
+    }
+
+    fn destroy(self: *PtyReactor) void {
+        self.shutdown.store(true, .release);
+        self.platform.wake();
+        self.thread.join();
+        for (self.entries.items) |entry| self.allocator.destroy(entry);
+        self.entries.deinit(self.allocator);
+        self.platform.deinit();
+        const allocator = self.allocator;
+        allocator.destroy(self);
+    }
+
+    fn add(self: *PtyReactor, shared: *PtyShared, transport: pty_transport.Pty, generation: u64) ?*PtyReactorEntry {
+        const entry = self.allocator.create(PtyReactorEntry) catch return null;
+        entry.* = .{ .shared = shared, .transport = transport, .generation = generation };
+        self.mutex.lock();
+        self.entries.append(self.allocator, entry) catch {
+            self.mutex.unlock();
+            self.allocator.destroy(entry);
+            return null;
+        };
+        self.mutex.unlock();
+        if (!self.platform.register(transport, @ptrCast(entry))) {
+            // kevent may apply an earlier changelist item before a later one
+            // fails. Delete both filters, then keep the inactive token alive
+            // through the next completed batch so stale udata stays valid.
+            self.platform.remove(transport);
+            entry.reclaim_after.store(self.epoch.load(.acquire) + 1, .release);
+            self.platform.wake();
+            return null;
+        }
+        entry.active.store(true, .release);
+        self.platform.wake();
+        return entry;
+    }
+
+    fn remove(self: *PtyReactor, entry: *PtyReactorEntry) void {
+        if (entry.active.swap(false, .acq_rel)) self.platform.remove(entry.transport);
+        // Two completed kevent batches fence every event that could still
+        // carry this udata pointer. Reclamation may wait longer while idle,
+        // but never races a stale kernel event.
+        entry.reclaim_after.store(self.epoch.load(.acquire) + 2, .release);
+        self.platform.wake();
+    }
+
+    fn resumeRead(self: *PtyReactor, entry: *PtyReactorEntry) void {
+        if (!entry.active.load(.acquire)) return;
+        self.platform.setReadable(entry.transport, true, @ptrCast(entry));
+        self.platform.wake();
+    }
+
+    fn enableWrite(self: *PtyReactor, entry: *PtyReactorEntry) void {
+        if (!entry.active.load(.acquire)) return;
+        self.platform.setWritable(entry.transport, true, @ptrCast(entry));
+        self.platform.wake();
+    }
+
+    fn notify(self: *PtyReactor) void {
+        self.platform.wake();
+    }
+
+    fn run(self: *PtyReactor) void {
+        var events: [64]pty_transport.ReactorEvent = undefined;
+        while (!self.shutdown.load(.acquire)) {
+            const count = self.platform.wait(&events);
+            if (self.shutdown.load(.acquire)) break;
+            var index: usize = 0;
+            while (index < count) : (index += 1) {
+                const event = events[index];
+                if (event.control) {
+                    self.sweepControl();
+                    continue;
+                }
+                const raw = event.token orelse continue;
+                const entry: *PtyReactorEntry = @ptrCast(@alignCast(raw));
+                if (!entry.active.load(.acquire)) continue;
+                self.service(entry, event);
+            }
+            const epoch = self.epoch.fetchAdd(1, .acq_rel) + 1;
+            self.reclaim(epoch);
+        }
+    }
+
+    fn reclaim(self: *PtyReactor, epoch: u64) void {
+        self.mutex.lock();
+        defer self.mutex.unlock();
+        var index: usize = 0;
+        while (index < self.entries.items.len) {
+            const entry = self.entries.items[index];
+            const after = entry.reclaim_after.load(.acquire);
+            if (after == 0 or after > epoch) {
+                index += 1;
+                continue;
+            }
+            _ = self.entries.swapRemove(index);
+            self.allocator.destroy(entry);
+        }
+    }
+
+    fn sweepControl(self: *PtyReactor) void {
+        self.mutex.lock();
+        defer self.mutex.unlock();
+        for (self.entries.items) |entry| {
+            if (!entry.active.load(.acquire)) continue;
+            const shared = entry.shared;
+            shared.mutex.lock();
+            const stop = shared.shutdown or shared.kill_requested or shared.generation != entry.generation;
+            const has_output_room = if (shared.staging) |staging| staging.len < max_effect_pty_staging_bytes else false;
+            const wants_write = shared.out_len > 0;
+            if (has_output_room) shared.reader_parked = false;
+            shared.mutex.unlock();
+            if (stop) {
+                self.finish(entry);
+            } else {
+                if (has_output_room) self.platform.setReadable(entry.transport, true, @ptrCast(entry));
+                self.platform.setWritable(entry.transport, wants_write, @ptrCast(entry));
+            }
+        }
+    }
+
+    fn service(self: *PtyReactor, entry: *PtyReactorEntry, event: pty_transport.ReactorEvent) void {
+        const shared = entry.shared;
+        shared.mutex.lock();
+        const stop = shared.shutdown or shared.kill_requested or shared.generation != entry.generation;
+        const staged = if (shared.staging) |staging| staging.len else max_effect_pty_staging_bytes;
+        const room = max_effect_pty_staging_bytes - staged;
+        if (room == 0 and !stop) shared.reader_parked = true;
+        shared.mutex.unlock();
+        if (stop) return self.finish(entry);
+
+        if (event.writable) {
+            ptyFlushOutbound(shared, entry.transport, entry.generation);
+            shared.mutex.lock();
+            const pending = shared.out_len > 0;
+            shared.mutex.unlock();
+            if (!pending) self.platform.setWritable(entry.transport, false, @ptrCast(entry));
+        }
+
+        if (event.readable and room > 0) {
+            var local: [16 * 1024]u8 = undefined;
+            const take = @min(local.len, room);
+            const read = entry.transport.read(local[0..take]) catch |err| switch (err) {
+                error.WouldBlock => return,
+                error.ReadFailed => return self.finish(entry),
+            };
+            if (read == 0) return self.finish(entry);
+            ptyStageOutput(shared, entry.generation, local[0..read]);
+            return;
+        }
+        if (room == 0) {
+            self.platform.setReadable(entry.transport, false, @ptrCast(entry));
+            return;
+        }
+        if (event.hangup) self.finish(entry);
+    }
+
+    fn finish(self: *PtyReactor, entry: *PtyReactorEntry) void {
+        if (!entry.active.swap(false, .acq_rel)) return;
+        self.platform.remove(entry.transport);
+        const thread = std.Thread.spawn(.{}, reapAndStage, .{entry}) catch {
+            // Resource exhaustion while ending a session must not strand its
+            // key. Reap synchronously as the rare fallback.
+            return reapAndStage(entry);
+        };
+        thread.detach();
+    }
+
+    fn reapAndStage(entry: *PtyReactorEntry) void {
+        const shared = entry.shared;
+        const generation = entry.generation;
+        const transport = entry.transport;
+        shared.mutex.lock();
+        if (shared.generation == generation) shared.reaping = true;
+        shared.mutex.unlock();
+        const exit = transport.reapEnding();
+        pty_transport.reapSurrendered();
+        const late_exec_failure = transport.lateExecFailure();
+        shared.mutex.lock();
+        if (shared.open and shared.generation == generation) {
+            if (shared.out_len > 0) {
+                shared.dropped_writes +|= @intCast(shared.out_write_count);
+                shared.out_len = 0;
+                shared.out_write_count = 0;
+                shared.out_front_sent = 0;
+            }
+            const cancelled = shared.kill_requested;
+            shared.exit_reason = if (cancelled)
+                .cancelled
+            else if (late_exec_failure)
+                .spawn_failed
+            else if (exit.signal != 0)
+                .signaled
+            else
+                .exited;
+            shared.exit_signal = if (shared.exit_reason == .signaled) exit.signal else 0;
+            shared.exit_code = if (shared.exit_reason == .exited) exit.code else effect_error_exit_code;
+            shared.exit_staged = true;
+            if (shared.owner) |owner| {
+                shared.exit_seq = owner.seq.fetchAdd(1, .seq_cst);
+                _ = owner.pending.fetchAdd(1, .seq_cst);
+            }
+        }
+        if (shared.generation == generation) shared.io_done = true;
+        shared.mutex.unlock();
+        ChannelHandle.requestHostWake(&shared.wake, generation);
+    }
+};
+
 /// `std.Io.Threaded` (the real executor's io) does not exist on
 /// freestanding targets — the docs' live-preview wasm host compiles
 /// this runtime to wasm32-freestanding. There the executor io compiles
@@ -3453,6 +3829,12 @@ pub fn Effects(comptime Msg: type) type {
             /// environ (`bindEnviron`), nothing else — a pty is a spawn
             /// with a different transport, one env story.
             term: []const u8 = pty_transport.default_term,
+            /// Caller-provided environment overlay. Entries replace a
+            /// bound-host value with the same name or append a new value;
+            /// names and values are copied into the pty slot before the
+            /// request is accepted. TERM is reserved for `term` and must
+            /// not appear here.
+            env: []const PtyEnvEntry = &.{},
             /// Every pty event — each coalesced output batch and the
             /// exactly-one `.exit` terminal — arrives through this
             /// constructor. Required: a terminal nobody watches is not
@@ -3469,6 +3851,7 @@ pub fn Effects(comptime Msg: type) type {
             cols: u16,
             rows: u16,
             term: []const u8,
+            env: []const PtyEnvEntry = &.{},
         };

         /// How one pty table slot advances: `.running` from the
@@ -3496,6 +3879,8 @@ pub fn Effects(comptime Msg: type) type {
             transport: if (pty_transport.supported) ?pty_transport.Pty else void =
                 if (pty_transport.supported) null else {},
             io_thread: ?WorkerThread = null,
+            reactor_entry: if (pty_transport.reactor_supported) ?*PtyReactorEntry else void =
+                if (pty_transport.reactor_supported) null else {},
             /// The io thread's nudge pipe: [read, write]. Written by
             /// the loop (outbound bytes staged, drain freed staging
             /// room, shutdown); poll()'d by the io thread.
@@ -3518,6 +3903,16 @@ pub fn Effects(comptime Msg: type) type {
             argv_count: usize = 0,
             term_storage: [max_effect_pty_term_bytes]u8 = undefined,
             term_len: usize = 0,
+            /// Caller-provided environment overlay. Names and values are
+            /// copied here so a caller may reuse its frame/request buffers
+            /// as soon as `ptySpawn` returns. Real starts wipe this storage
+            /// after the transport has consumed it; fake/replay starts keep
+            /// it until retirement so `pendingPtyAt` can inspect the exact
+            /// request.
+            env_storage: [max_effect_pty_env_bytes]u8 = undefined,
+            env_entries: [max_effect_pty_env_entries]PtyEnvEntry = undefined,
+            env_count: usize = 0,
+            env_bytes_len: usize = 0,
             /// Session replay only: the pending-order reservation this
             /// parked spawn holds (see `ParkOrderState` — the channel
             /// park dance, pty-shaped: a fed start-failure terminal
@@ -3534,6 +3929,37 @@ pub fn Effects(comptime Msg: type) type {
             fn requestTerm(slot: *const PtySlot) []const u8 {
                 return slot.term_storage[0..slot.term_len];
             }
+
+            fn requestEnv(slot: *const PtySlot) []const PtyEnvEntry {
+                return slot.env_entries[0..slot.env_count];
+            }
+
+            fn copyEnv(slot: *PtySlot, env: []const PtyEnvEntry) void {
+                var offset: usize = 0;
+                for (env, 0..) |entry, index| {
+                    const name_start = offset;
+                    @memcpy(slot.env_storage[name_start .. name_start + entry.name.len], entry.name);
+                    offset += entry.name.len;
+                    const value_start = offset;
+                    @memcpy(slot.env_storage[value_start .. value_start + entry.value.len], entry.value);
+                    offset += entry.value.len;
+                    slot.env_entries[index] = .{
+                        .name = slot.env_storage[name_start .. name_start + entry.name.len],
+                        .value = slot.env_storage[value_start .. value_start + entry.value.len],
+                    };
+                }
+                slot.env_count = env.len;
+                slot.env_bytes_len = offset;
+            }
+
+            fn clearEnv(slot: *PtySlot) void {
+                // The caller's overlay may contain credentials or tokens;
+                // ordinary allocator reuse must not retain those bytes in
+                // a later pty slot or expose them through a heap dump.
+                std.crypto.secureZero(u8, slot.env_storage[0..]);
+                slot.env_count = 0;
+                slot.env_bytes_len = 0;
+            }
         };

         /// How one channel table slot advances: `.open` accepts posts
@@ -5026,7 +5452,12 @@ pub fn Effects(comptime Msg: type) type {
         /// occupancies beside the channel table. Loop-thread only —
         /// the thread-shared half of each slot lives behind its
         /// `PtySlot.shared` header.
-        pty_slots: [max_effect_ptys]PtySlot = [_]PtySlot{.{}} ** max_effect_ptys,
+        pty_slots: std.ArrayListUnmanaged(PtySlot) = .empty,
+        /// macOS serves all live PTYs from one kqueue thread. Other hosts keep
+        /// the existing per-session transport path until their epoll/IOCP
+        /// reactors are implemented.
+        pty_reactor: if (pty_transport.reactor_supported) ?*PtyReactor else void =
+            if (pty_transport.reactor_supported) null else {},
         /// Monotonic stamp shared by every pty's staged output backlog
         /// and exit marker — the channels' `channel_seq`, pty-shaped:
         /// cross-pty delivery order and the drain boundary's causality
@@ -5273,7 +5704,7 @@ pub fn Effects(comptime Msg: type) type {
             // teaching warning: everything it can still reach (the
             // shared header, its staging ring, the transport fds) is
             // process-lived or deliberately leaked with it.
-            for (&self.pty_slots) |*pty_slot| {
+            for (self.pty_slots.items) |*pty_slot| {
                 // Wind down a running REAL pty: kill and wait (bounded)
                 // for the io thread to stage its exit, then retire or
                 // abandon it. A fake or unsupported occupancy just
@@ -5298,7 +5729,11 @@ pub fn Effects(comptime Msg: type) type {
                         } else if (pty_slot.transport) |transport| {
                             transport.kill(false);
                         }
-                        pty_transport.nudge(pty_slot.wake_pipe[1]);
+                        if (comptime pty_transport.reactor_supported) {
+                            if (self.pty_reactor) |reactor| reactor.notify();
+                        } else {
+                            pty_transport.nudge(pty_slot.wake_pipe[1]);
+                        }
                         var io_done = pty_slot.shared == null;
                         if (pty_slot.shared) |shared| {
                             const start_ns = runtime_clock.monotonicNanoseconds();
@@ -5375,7 +5810,18 @@ pub fn Effects(comptime Msg: type) type {
                     pty_slot.state = .idle;
                     pty_slot.on_event = null;
                 }
-            }
+                // No transport thread reads the overlay after the
+                // synchronous spawn call; clear it for every slot, including
+                // an abandoned or already-idle slot, before the slot array
+                // is freed.
+                pty_slot.clearEnv();
+            }
+            if (comptime pty_transport.reactor_supported) {
+                if (self.pty_reactor) |reactor| reactor.destroy();
+                self.pty_reactor = null;
+            }
+            self.pty_slots.deinit(self.allocator);
+            self.pty_slots = .empty;
             self.pty_pending_count.store(0, .release);
             // Settle any surrendered reap whose child has since died —
             // teardown is the last cheap moment before the process
@@ -6029,7 +6475,7 @@ pub fn Effects(comptime Msg: type) type {
                 // `bindServices`, which the standard startup order
                 // never does; contained here regardless.)
                 if (comptime pty_transport.supported) {
-                    for (&self.pty_slots) |*pty_slot| {
+                    for (self.pty_slots.items) |*pty_slot| {
                         if (!(pty_slot.state == .running and !pty_slot.fake)) continue;
                         const shared = pty_slot.shared orelse continue;
                         shared.mutex.lock();
@@ -6039,7 +6485,11 @@ pub fn Effects(comptime Msg: type) type {
                             if (pty_slot.transport) |transport| transport.kill(false);
                         }
                         shared.mutex.unlock();
-                        pty_transport.nudge(pty_slot.wake_pipe[1]);
+                        if (comptime pty_transport.reactor_supported) {
+                            if (self.pty_reactor) |reactor| reactor.notify();
+                        } else {
+                            pty_transport.nudge(pty_slot.wake_pipe[1]);
+                        }
                         const start_ns = runtime_clock.monotonicNanoseconds();
                         var io_done = false;
                         while (true) {
@@ -6133,7 +6583,7 @@ pub fn Effects(comptime Msg: type) type {
             }
             // Ptys arm the same producer-wake seam: a running REAL
             // occupancy's io thread wakes through its shared header.
-            for (&self.pty_slots) |*slot| {
+            for (self.pty_slots.items) |*slot| {
                 if (slot.state == .running and !slot.fake and slot.shared != null) return true;
             }
             return false;
@@ -8004,12 +8454,12 @@ pub fn Effects(comptime Msg: type) type {
         /// second producer beside a live io thread.
         pub fn feedPtyOutput(self: *Self, key: u64, bytes: []const u8) error{ EffectNotFound, EffectQueueFull, PtyLiveFeed, PtyChunkTooLarge, ReplayDamagedRecord }!void {
             const slot_index = blk: {
-                for (&self.pty_slots, 0..) |*slot, index| {
+                for (self.pty_slots.items, 0..) |*slot, index| {
                     if (slot.state != .idle and slot.key == key) break :blk index;
                 }
                 return error.EffectNotFound;
             };
-            const slot = &self.pty_slots[slot_index];
+            const slot = &self.pty_slots.items[slot_index];
             if (!slot.fake) return error.PtyLiveFeed;
             if (slot.park_state == .terminated) {
                 if (comptime builtin.os.tag != .freestanding) {
@@ -8072,12 +8522,12 @@ pub fn Effects(comptime Msg: type) type {
         /// ride the completion queue and retire the slot at delivery.
         pub fn feedPtyExit(self: *Self, key: u64, code: i32, signal: i32, reason: EffectExitReason, dropped_writes: u32) error{ EffectNotFound, EffectQueueFull, PtyLiveFeed, ReplayDamagedRecord }!void {
             const slot_index = blk: {
-                for (&self.pty_slots, 0..) |*slot, index| {
+                for (self.pty_slots.items, 0..) |*slot, index| {
                     if (slot.state != .idle and slot.key == key) break :blk index;
                 }
                 return error.EffectNotFound;
             };
-            const slot = &self.pty_slots[slot_index];
+            const slot = &self.pty_slots.items[slot_index];
             if (!slot.fake) return error.PtyLiveFeed;
             // Normalize the tuple to the EffectPtyEvent contract the live
             // io loop guarantees, so a fake (test or replay) can never
@@ -8204,7 +8654,7 @@ pub fn Effects(comptime Msg: type) type {
         /// Number of recorded (still-active) fake pty spawns.
         pub fn pendingPtyCount(self: *Self) usize {
             var count: usize = 0;
-            for (&self.pty_slots) |*slot| {
+            for (self.pty_slots.items) |*slot| {
                 if (slot.fake and slot.state == .running) count += 1;
             }
             return count;
@@ -8215,7 +8665,7 @@ pub fn Effects(comptime Msg: type) type {
         /// valid until the occupancy retires.
         pub fn pendingPtyAt(self: *Self, index: usize) ?PtyRequest {
             var seen: usize = 0;
-            for (&self.pty_slots) |*slot| {
+            for (self.pty_slots.items) |*slot| {
                 if (!(slot.fake and slot.state == .running)) continue;
                 if (seen == index) {
                     return .{
@@ -8224,6 +8674,7 @@ pub fn Effects(comptime Msg: type) type {
                         .cols = slot.cols,
                         .rows = slot.rows,
                         .term = slot.requestTerm(),
+                        .env = slot.requestEnv(),
                     };
                 }
                 seen += 1;
@@ -8291,16 +8742,12 @@ pub fn Effects(comptime Msg: type) type {
             // truncate it at the C boundary, so a spawn that "succeeded"
             // would hand the child a different TERM than requested.
             if (std.mem.indexOfScalar(u8, options.term, 0) != null) return self.rejectPty(options.key, options.on_event, true);
+            if (!validPtyEnvOverlay(options.env)) return self.rejectPty(options.key, options.on_event, true);
             if (self.keyOccupiedUntilDelivery(options.key)) return self.rejectPty(options.key, options.on_event, true);
-            const slot_index = self.findIdlePtySlot() orelse return self.rejectPty(options.key, options.on_event, true);
-            // Table capacity obeys the replay-hold invariant, the
-            // channel argument verbatim: executor-truth start failures
-            // park a real slot under replay until the journaled
-            // terminal feeds, so live holds the open counted through
-            // the staged window too.
-            if (self.idlePtySlotCount() <= self.stagedPtyReservationCount()) {
-                return self.rejectPty(options.key, options.on_event, true);
-            }
+            // Grow on demand. Allocation failure is executor truth for this
+            // host and rejects only the requested session; existing PTYs keep
+            // running.
+            const slot_index = self.claimIdlePtySlot() orelse return self.rejectPty(options.key, options.on_event, false);
             // A build without a pty transport (a libc-free Linux
             // build; a target outside the support matrix) refuses up
             // front. Unlike
@@ -8317,7 +8764,7 @@ pub fn Effects(comptime Msg: type) type {
                 if (self.executor != .fake) return self.rejectPty(options.key, options.on_event, false);
             }

-            const slot = &self.pty_slots[slot_index];
+            const slot = &self.pty_slots.items[slot_index];
             const generation = self.nextChannelGeneration();
             slot.* = .{
                 .state = .running,
@@ -8338,6 +8785,11 @@ pub fn Effects(comptime Msg: type) type {
             }
             @memcpy(slot.term_storage[0..options.term.len], options.term);
             slot.term_len = options.term.len;
+            slot.copyEnv(options.env);
+
+            if (self.pty_tap) |tap| if (tap.spawn) |notify_spawn| {
+                notify_spawn(tap.context, options.key);
+            };

             if (slot.fake) {
                 // Session replay: PARK — the fake-slot discipline. The
@@ -8496,7 +8948,13 @@ pub fn Effects(comptime Msg: type) type {
                 break :accepted true;
             };
             shared.mutex.unlock();
-            if (accepted) pty_transport.nudge(slot.wake_pipe[1]);
+            if (accepted) {
+                if (comptime pty_transport.reactor_supported) {
+                    if (slot.reactor_entry) |entry| if (self.pty_reactor) |reactor| reactor.enableWrite(entry);
+                } else {
+                    pty_transport.nudge(slot.wake_pipe[1]);
+                }
+            }
             return accepted;
         }

@@ -8574,7 +9032,20 @@ pub fn Effects(comptime Msg: type) type {
                 if (slot.transport) |transport| transport.kill(false);
             }
             shared.mutex.unlock();
-            pty_transport.nudge(slot.wake_pipe[1]);
+            if (comptime pty_transport.reactor_supported) {
+                if (self.pty_reactor) |reactor| reactor.notify();
+            } else {
+                pty_transport.nudge(slot.wake_pipe[1]);
+            }
+        }
+
+        /// Forget framework-owned emulator state for a retired PTY key. The
+        /// external process lifecycle remains owned by `ptyKill` + its exit
+        /// event; this synchronous hook only releases scrollback/render buffers.
+        pub fn ptyForget(self: *Self, key: u64) void {
+            if (self.pty_tap) |tap| if (tap.forget) |forget| {
+                forget(tap.context, key);
+            };
         }

         /// Ask the desktop host to show a system notification. This is
@@ -11444,7 +11915,7 @@ pub fn Effects(comptime Msg: type) type {
                 shared.wake.pending.store(false, .seq_cst);
                 shared.wake.mutex.unlock();
             }
-            for (&self.pty_slots) |*slot| {
+            for (self.pty_slots.items) |*slot| {
                 const shared = slot.shared orelse continue;
                 shared.wake.mutex.lock();
                 shared.wake.pending.store(false, .seq_cst);
@@ -11779,7 +12250,7 @@ pub fn Effects(comptime Msg: type) type {
                             // still lands in the delivered count.
                             var event = entry.event;
                             if (entry.retire_slot) |slot_index| {
-                                const parked = &self.pty_slots[slot_index];
+                                const parked = &self.pty_slots.items[slot_index];
                                 if (parked.state != .idle and parked.generation == entry.retire_generation) {
                                     event.dropped_writes +|= parked.dropped_writes;
                                     self.retirePtySlot(parked);
@@ -12377,7 +12848,7 @@ pub fn Effects(comptime Msg: type) type {
                             self.drain_heap_line = heap;
                             entry.heap_line = null;
                         }
-                        const pty_slot = &self.pty_slots[entry.slot_index];
+                        const pty_slot = &self.pty_slots.items[entry.slot_index];
                         if (pty_slot.state == .idle or entry.channel_generation != pty_slot.generation) continue;
                         const on_event = pty_slot.on_event;
                         // The delivered drop count reads AT DELIVERY, the
@@ -12556,7 +13027,7 @@ pub fn Effects(comptime Msg: type) type {
             var best_index: ?usize = null;
             var best_seq: u64 = std.math.maxInt(u64);
             var best_exit = false;
-            for (&self.pty_slots, 0..) |*slot, index| {
+            for (self.pty_slots.items, 0..) |*slot, index| {
                 if (slot.state == .idle) continue;
                 const shared = slot.shared orelse continue;
                 shared.mutex.lock();
@@ -12603,7 +13074,7 @@ pub fn Effects(comptime Msg: type) type {
                 }
             }
             const index = best_index orelse return null;
-            const slot = &self.pty_slots[index];
+            const slot = &self.pty_slots.items[index];
             const shared = slot.shared.?;
             const on_event = slot.on_event;
             var event: EffectPtyEvent = .{ .key = slot.key };
@@ -12641,7 +13112,6 @@ pub fn Effects(comptime Msg: type) type {
                 staging.head = (staging.head + take) % max_effect_pty_staging_bytes;
                 staging.len -= take;
                 const emptied = staging.len == 0;
-                var rewake = false;
                 if (emptied) {
                     shared.data_seq_valid = false;
                 } else if (shared.owner) |owner| {
@@ -12654,7 +13124,6 @@ pub fn Effects(comptime Msg: type) type {
                     // stamp would let one pass deliver without bound and
                     // starve every other platform event.
                     shared.data_seq = owner.seq.fetchAdd(1, .seq_cst);
-                    rewake = true;
                 }
                 if (shared.reader_parked) {
                     shared.reader_parked = false;
@@ -12662,9 +13131,12 @@ pub fn Effects(comptime Msg: type) type {
                 }
                 shared.mutex.unlock();
                 if (emptied) _ = self.pty_pending_count.fetchSub(1, .seq_cst);
-                if (rewake) ChannelHandle.requestHostWake(&shared.wake, slot.generation);
                 if (resume_reader and comptime pty_transport.supported) {
-                    pty_transport.nudge(slot.wake_pipe[1]);
+                    if (comptime pty_transport.reactor_supported) {
+                        if (slot.reactor_entry) |entry| if (self.pty_reactor) |reactor| reactor.resumeRead(entry);
+                    } else {
+                        pty_transport.nudge(slot.wake_pipe[1]);
+                    }
                 }
                 event.bytes = self.pty_drain_scratch[0..take];
                 self.journalNote(.{
@@ -13857,22 +14329,23 @@ pub fn Effects(comptime Msg: type) type {
         }

         fn findPtySlot(self: *Self, key: u64) ?*PtySlot {
-            for (&self.pty_slots) |*slot| {
+            for (self.pty_slots.items) |*slot| {
                 if (slot.state != .idle and slot.key == key) return slot;
             }
             return null;
         }

-        fn findIdlePtySlot(self: *Self) ?usize {
-            for (&self.pty_slots, 0..) |*slot, index| {
+        fn claimIdlePtySlot(self: *Self) ?usize {
+            for (self.pty_slots.items, 0..) |*slot, index| {
                 if (slot.state == .idle) return index;
             }
-            return null;
+            self.pty_slots.append(self.allocator, .{}) catch return null;
+            return self.pty_slots.items.len - 1;
         }

         fn idlePtySlotCount(self: *Self) usize {
             var count: usize = 0;
-            for (&self.pty_slots) |*slot| {
+            for (self.pty_slots.items) |*slot| {
                 if (slot.state == .idle) count += 1;
             }
             return count;
@@ -13937,6 +14410,10 @@ pub fn Effects(comptime Msg: type) type {
         /// wait on child I/O). The `PtyShared` header stays allocated
         /// forever, the channel headers' bounded-leak invariant.
         fn retirePtySlot(self: *Self, slot: *PtySlot) void {
+            if (comptime pty_transport.reactor_supported) {
+                if (slot.reactor_entry) |entry| if (self.pty_reactor) |reactor| reactor.remove(entry);
+                slot.reactor_entry = null;
+            }
             if (slot.shared) |shared| {
                 shared.mutex.lock();
                 shared.open = false;
@@ -14012,6 +14489,7 @@ pub fn Effects(comptime Msg: type) type {
             slot.kill_requested = false;
             slot.write_capture_len = 0;
             slot.dropped_writes = 0;
+            slot.clearEnv();
             slot.park_seq = 0;
             slot.park_state = .none;
         }
@@ -14036,6 +14514,10 @@ pub fn Effects(comptime Msg: type) type {
         /// NFS/autofs PATH entry stalls the loop like any synchronous
         /// filesystem touch of that mount would.
         fn startRealPty(self: *Self, slot: *PtySlot, options: PtySpawnOptions) void {
+            // The overlay is needed only while the synchronous transport
+            // builds its argv/envp and forks. Do not leave caller-supplied
+            // secrets in the slot after success or any start failure.
+            defer slot.clearEnv();
             if (comptime !pty_transport.supported) {
                 // ptySpawn already refused unsupported builds.
                 unreachable;
@@ -14057,17 +14539,25 @@ pub fn Effects(comptime Msg: type) type {
                 return self.failPtyStart(slot, options);
             };
             outbound.* = .{};
-            const pipe = pty_transport.pipePair() catch {
-                self.channel_storage_allocator.destroy(outbound);
-                self.channel_storage_allocator.destroy(staging);
-                return self.failPtyStart(slot, options);
-            };
+            const pipe: [2]c_int = if (comptime pty_transport.reactor_supported)
+                .{ -1, -1 }
+            else
+                pty_transport.pipePair() catch {
+                    self.channel_storage_allocator.destroy(outbound);
+                    self.channel_storage_allocator.destroy(staging);
+                    return self.failPtyStart(slot, options);
+                };
             var env_buffer: [pty_transport.max_env_entries]pty_transport.EnvVar = undefined;
             // The decoded-name/value byte pool behind the Windows
             // environment snapshot; zero-length (and untouched) where
             // the posix block's own bytes back the entries.
             var env_bytes: [if (builtin.os.tag == .windows) pty_transport.max_env_bytes else 0]u8 = undefined;
-            const env = flattenPtyEnviron(self.environ orelse fallbackEnviron(), &env_buffer, &env_bytes) orelse {
+            const env = flattenPtyEnviron(
+                self.environ orelse fallbackEnviron(),
+                &env_buffer,
+                &env_bytes,
+                slot.requestEnv(),
+            ) orelse {
                 self.channel_storage_allocator.destroy(outbound);
                 self.channel_storage_allocator.destroy(staging);
                 pty_transport.closeFd(pipe[0]);
@@ -14152,6 +14642,45 @@ pub fn Effects(comptime Msg: type) type {
                 }
                 if (self.hasPending()) self.wakeHost();
             }
+            if (comptime pty_transport.reactor_supported) {
+                const reactor = self.pty_reactor orelse created: {
+                    const created = PtyReactor.create(self.allocator) orelse {
+                        shared.mutex.lock();
+                        shared.open = false;
+                        shared.staging = null;
+                        shared.outbound = null;
+                        shared.owner = null;
+                        shared.mutex.unlock();
+                        revokeChannelWake(&shared.wake);
+                        self.channel_storage_allocator.destroy(staging);
+                        self.channel_storage_allocator.destroy(outbound);
+                        transport.kill(false);
+                        _ = transport.reapEnding();
+                        transport.close();
+                        return self.failPtyStart(slot, options);
+                    };
+                    self.pty_reactor = created;
+                    break :created created;
+                };
+                const entry = reactor.add(shared, transport, generation) orelse {
+                    shared.mutex.lock();
+                    shared.open = false;
+                    shared.staging = null;
+                    shared.outbound = null;
+                    shared.owner = null;
+                    shared.mutex.unlock();
+                    revokeChannelWake(&shared.wake);
+                    self.channel_storage_allocator.destroy(staging);
+                    self.channel_storage_allocator.destroy(outbound);
+                    transport.kill(false);
+                    _ = transport.reapEnding();
+                    transport.close();
+                    return self.failPtyStart(slot, options);
+                };
+                slot.transport = transport;
+                slot.reactor_entry = entry;
+                return;
+            }
             const thread = std.Thread.spawn(.{}, ptyIoLoop, .{ shared, transport, pipe[0], generation }) catch {
                 shared.mutex.lock();
                 shared.open = false;
@@ -14181,6 +14710,7 @@ pub fn Effects(comptime Msg: type) type {
         /// Release a claimed pty slot and stage the executor-truth
         /// start-failure terminal.
         fn failPtyStart(self: *Self, slot: *PtySlot, options: PtySpawnOptions) void {
+            slot.clearEnv();
             slot.state = .idle;
             slot.on_event = null;
             self.stagePendingPty(.{
diff --git a/node_modules/@native-sdk/cli/src/runtime/effects_pty_tests.zig b/node_modules/@native-sdk/cli/src/runtime/effects_pty_tests.zig
index a92c59f..5d011e4 100644
--- a/node_modules/@native-sdk/cli/src/runtime/effects_pty_tests.zig
+++ b/node_modules/@native-sdk/cli/src/runtime/effects_pty_tests.zig
@@ -46,6 +46,26 @@ const DirectMsg = union(enum) {

 const DirectFx = effects_mod.Effects(DirectMsg);

+const PtyTapRecorder = struct {
+    spawned: usize = 0,
+    forgotten: usize = 0,
+    last_key: u64 = 0,
+
+    fn notify(_: *anyopaque, _: *const effects_mod.EffectPtyEvent) void {}
+
+    fn spawn(context: *anyopaque, key: u64) void {
+        const recorder: *PtyTapRecorder = @ptrCast(@alignCast(context));
+        recorder.spawned += 1;
+        recorder.last_key = key;
+    }
+
+    fn forget(context: *anyopaque, key: u64) void {
+        const recorder: *PtyTapRecorder = @ptrCast(@alignCast(context));
+        recorder.forgotten += 1;
+        recorder.last_key = key;
+    }
+};
+
 fn expectOutput(fx: *DirectFx, key: u64, bytes: []const u8) !void {
     const msg = fx.takeMsg() orelse return error.TestExpectedMsg;
     try testing.expectEqual(key, msg.pty.key);
@@ -70,13 +90,21 @@ test "fake pty lifecycle: spawn parks the request, feeds deliver, exit retires t
     defer fx.deinit();
     fx.executor = .fake;

+    var overlay_name: [15]u8 = "PTY_TEST_MARKER".*;
+    var overlay_value: [7]u8 = "overlay".*;
+
     fx.ptySpawn(.{
         .key = 9,
         .argv = &.{ "sh", "-l" },
         .cols = 120,
         .rows = 40,
+        .env = &.{.{ .name = overlay_name[0..], .value = overlay_value[0..] }},
         .on_event = DirectFx.ptyMsg(.pty),
     });
+    // The caller can recycle its request buffers immediately after the
+    // effect call; the fake request must retain the original bytes.
+    overlay_name[0] = 'X';
+    overlay_value[0] = 'X';
     try testing.expectEqual(@as(usize, 1), fx.pendingPtyCount());
     const request = fx.pendingPtyAt(0) orelse return error.TestExpectedRequest;
     try testing.expectEqual(@as(u64, 9), request.key);
@@ -86,6 +114,11 @@ test "fake pty lifecycle: spawn parks the request, feeds deliver, exit retires t
     try testing.expectEqual(@as(u16, 120), request.cols);
     try testing.expectEqual(@as(u16, 40), request.rows);
     try testing.expectEqualStrings(pty_transport.default_term, request.term);
+    // Environment entries are their own copied request storage, never
+    // appended to argv (which remains exactly the caller's two entries).
+    try testing.expectEqual(@as(usize, 1), request.env.len);
+    try testing.expectEqualStrings("PTY_TEST_MARKER", request.env[0].name);
+    try testing.expectEqualStrings("overlay", request.env[0].value);

     try fx.feedPtyOutput(9, "$ ");
     try fx.feedPtyOutput(9, "hello\r\n");
@@ -102,6 +135,31 @@ test "fake pty lifecycle: spawn parks the request, feeds deliver, exit retires t
     try testing.expectEqual(@as(usize, 1), fx.pendingPtyCount());
 }

+test "pty presentation lifecycle observes accepted spawn and explicit forget" {
+    var fx = DirectFx.init(testing.allocator);
+    defer fx.deinit();
+    fx.executor = .fake;
+    var recorder: PtyTapRecorder = .{};
+    fx.pty_tap = .{
+        .context = @ptrCast(&recorder),
+        .notify = PtyTapRecorder.notify,
+        .spawn = PtyTapRecorder.spawn,
+        .forget = PtyTapRecorder.forget,
+    };
+
+    fx.ptySpawn(.{ .key = 61, .argv = &.{"sh"}, .on_event = DirectFx.ptyMsg(.pty) });
+    try testing.expectEqual(@as(usize, 1), recorder.spawned);
+    try testing.expectEqual(@as(u64, 61), recorder.last_key);
+
+    // A rejected duplicate never claims a fresh terminal lifetime.
+    fx.ptySpawn(.{ .key = 61, .argv = &.{"sh"}, .on_event = DirectFx.ptyMsg(.pty) });
+    try testing.expectEqual(@as(usize, 1), recorder.spawned);
+
+    fx.ptyForget(61);
+    try testing.expectEqual(@as(usize, 1), recorder.forgotten);
+    try testing.expectEqual(@as(u64, 61), recorder.last_key);
+}
+
 test "pty admission: every refused spawn delivers exactly one rejected exit" {
     var fx = DirectFx.init(testing.allocator);
     defer fx.deinit();
@@ -131,20 +189,39 @@ test "pty admission: every refused spawn delivers exactly one rejected exit" {
     fx.ptySpawn(.{ .key = 5, .argv = &.{"sh"}, .term = long_term, .on_event = DirectFx.ptyMsg(.pty) });
     _ = try expectExit(&fx, 5, .rejected);

+    // Environment names are map keys, not shell fragments: reject empty
+    // names, '=' and NUL in names/values, and TERM (owned by `.term`).
+    fx.ptySpawn(.{ .key = 100, .argv = &.{"sh"}, .env = &.{.{ .name = "", .value = "x" }}, .on_event = DirectFx.ptyMsg(.pty) });
+    _ = try expectExit(&fx, 100, .rejected);
+    fx.ptySpawn(.{ .key = 101, .argv = &.{"sh"}, .env = &.{.{ .name = "BAD=NAME", .value = "x" }}, .on_event = DirectFx.ptyMsg(.pty) });
+    _ = try expectExit(&fx, 101, .rejected);
+    fx.ptySpawn(.{ .key = 102, .argv = &.{"sh"}, .env = &.{.{ .name = "BAD\x00NAME", .value = "x" }}, .on_event = DirectFx.ptyMsg(.pty) });
+    _ = try expectExit(&fx, 102, .rejected);
+    fx.ptySpawn(.{ .key = 103, .argv = &.{"sh"}, .env = &.{.{ .name = "BAD_VALUE", .value = "x\x00y" }}, .on_event = DirectFx.ptyMsg(.pty) });
+    _ = try expectExit(&fx, 103, .rejected);
+    fx.ptySpawn(.{ .key = 104, .argv = &.{"sh"}, .env = &.{.{ .name = "TERM", .value = "dumb" }}, .on_event = DirectFx.ptyMsg(.pty) });
+    _ = try expectExit(&fx, 104, .rejected);
+
+    var too_many_env: [effects_mod.max_effect_pty_env_entries + 1]effects_mod.PtyEnvEntry = undefined;
+    for (&too_many_env) |*entry| entry.* = .{ .name = "X", .value = "y" };
+    fx.ptySpawn(.{ .key = 105, .argv = &.{"sh"}, .env = &too_many_env, .on_event = DirectFx.ptyMsg(.pty) });
+    _ = try expectExit(&fx, 105, .rejected);
+    const oversized_env_value = "z" ** effects_mod.max_effect_pty_env_bytes;
+    fx.ptySpawn(.{ .key = 106, .argv = &.{"sh"}, .env = &.{.{ .name = "OVERSIZED", .value = oversized_env_value }}, .on_event = DirectFx.ptyMsg(.pty) });
+    _ = try expectExit(&fx, 106, .rejected);
+
     // A duplicate active key.
     fx.ptySpawn(.{ .key = 6, .argv = &.{"sh"}, .on_event = DirectFx.ptyMsg(.pty) });
     fx.ptySpawn(.{ .key = 6, .argv = &.{"sh"}, .on_event = DirectFx.ptyMsg(.pty) });
     _ = try expectExit(&fx, 6, .rejected);

-    // Table exhaustion: the table already holds key 6; filling the
-    // remaining slots leaves the next spawn refused.
+    // Dynamic growth: the table already holds key 6 and accepts a burst
+    // far beyond the historical fixed capacity.
     var key: u64 = 7;
-    while (key < 7 + effects_mod.max_effect_ptys - 1) : (key += 1) {
+    while (key < 71) : (key += 1) {
         fx.ptySpawn(.{ .key = key, .argv = &.{"sh"}, .on_event = DirectFx.ptyMsg(.pty) });
     }
-    fx.ptySpawn(.{ .key = 99, .argv = &.{"sh"}, .on_event = DirectFx.ptyMsg(.pty) });
-    _ = try expectExit(&fx, 99, .rejected);
-    try testing.expectEqual(@as(usize, effects_mod.max_effect_ptys), fx.pendingPtyCount());
+    try testing.expectEqual(@as(usize, 65), fx.pendingPtyCount());
 }

 test "fake pty write capture, resize mirror, and kill mirror" {
@@ -542,13 +619,13 @@ test "a fed output batch over the chunk bound is refused, never truncated" {
     try testing.expectError(error.PtyChunkTooLarge, fx.feedPtyOutput(72, &over));
 }

-test "a fed output batch past the inline entry bound rides a heap payload intact" {
+test "a fed output batch at the chunk bound arrives intact" {
     var fx = DirectFx.init(testing.allocator);
     defer fx.deinit();
     fx.executor = .fake;

     fx.ptySpawn(.{ .key = 21, .argv = &.{"sh"}, .on_event = DirectFx.ptyMsg(.pty) });
-    var big: [effects_mod.max_effect_line_bytes + 128]u8 = undefined;
+    var big: [effects_mod.max_effect_pty_chunk_bytes]u8 = undefined;
     for (&big, 0..) |*byte, index| byte.* = @intCast('a' + (index % 26));
     try fx.feedPtyOutput(21, &big);
     const msg = fx.takeMsg() orelse return error.TestExpectedMsg;
@@ -676,6 +753,40 @@ test "live pty end to end: output, coalescing, and the exit code" {
     try testing.expectEqual(effects_mod.EffectExitReason.exited, result.exit.reason);
 }

+test "live pty env overlay replaces host values and is not argv" {
+    if (comptime !live_posix) return;
+    const host_marker = try testing.allocator.dupeZ(u8, "CANOPY_PTY_OVERLAY=host");
+    defer testing.allocator.free(host_marker);
+    const host_base = try testing.allocator.dupeZ(u8, "CANOPY_PTY_BASE=host");
+    defer testing.allocator.free(host_base);
+    // Bind a small deterministic host environment so the replacement is
+    // observable without mutating the process-global environ. The overlay
+    // changes one value and appends another; `PATH` is unnecessary because
+    // the test uses an absolute shell path.
+    const host_entries = [_]?[*:0]const u8{ host_marker.ptr, host_base.ptr, null };
+
+    var fx = DirectFx.init(testing.allocator);
+    defer fx.deinit();
+    fx.bindEnviron(.{ .block = .{ .slice = host_entries[0..2 :null] } });
+    fx.ptySpawn(.{
+        .key = 55,
+        .argv = &.{ "/bin/sh", "-c", "printf '%s|%s|%s' \"$CANOPY_PTY_OVERLAY\" \"$CANOPY_PTY_BASE\" \"$CANOPY_PTY_NEW\"" },
+        .env = &.{
+            .{ .name = "CANOPY_PTY_OVERLAY", .value = "overlay" },
+            .{ .name = "CANOPY_PTY_NEW", .value = "new" },
+        },
+        .on_event = DirectFx.ptyMsg(.pty),
+    });
+    var result = try drainUntilExit(&fx, 5_000);
+    defer result.output.deinit(testing.allocator);
+    // The first value proves replacement over the bound host; the second
+    // proves the inherited value remains, and the third proves a new
+    // overlay entry reaches the child. Since all are sent through `.env`,
+    // none can be mistaken for an argv token.
+    try testing.expect(std.mem.indexOf(u8, result.output.items, "overlay|host|new") != null);
+    try testing.expectEqual(effects_mod.EffectExitReason.exited, result.exit.reason);
+}
+
 test "live pty back-pressure is lossless past the staging ring" {
     if (comptime !live_posix) return;
     var fx = DirectFx.init(testing.allocator);
diff --git a/node_modules/@native-sdk/cli/src/runtime/pty.zig b/node_modules/@native-sdk/cli/src/runtime/pty.zig
index 4ed1f37..b4165cc 100644
--- a/node_modules/@native-sdk/cli/src/runtime/pty.zig
+++ b/node_modules/@native-sdk/cli/src/runtime/pty.zig
@@ -963,6 +963,15 @@ const c = struct {
     extern "c" fn pipe2(fds: *[2]c_int, flags: c_uint) c_int;
     extern "c" fn fcntl(fd: c_int, cmd: c_int, ...) c_int;
     extern "c" fn poll(fds: [*]Pollfd, nfds: c_uint, timeout: c_int) c_int;
+    extern "c" fn kqueue() c_int;
+    extern "c" fn kevent(
+        kq: c_int,
+        changelist: ?[*]const Kevent,
+        nchanges: c_int,
+        eventlist: ?[*]Kevent,
+        nevents: c_int,
+        timeout: ?*const anyopaque,
+    ) c_int;
     extern "c" fn __error() *c_int; // Darwin errno
     extern "c" fn __errno_location() *c_int; // Linux errno (glibc/musl)
     extern "c" fn __errno() *c_int; // Bionic errno
@@ -1087,6 +1096,124 @@ pub const Ready = struct {
     nudged: bool = false,
 };

+/// Darwin readiness reactor used by the effects layer to serve every live PTY
+/// from one blocking thread. kqueue registration and wake calls are safe from
+/// the loop thread while another thread is inside `kevent`.
+pub const reactor_supported = builtin.os.tag == .macos;
+
+pub const ReactorEvent = struct {
+    token: ?*anyopaque = null,
+    readable: bool = false,
+    writable: bool = false,
+    hangup: bool = false,
+    control: bool = false,
+};
+
+pub const Reactor = if (reactor_supported) struct {
+    fd: c_int,
+
+    const control_ident: usize = 1;
+
+    pub fn init() error{ReactorInitFailed}!Reactor {
+        const fd = c.kqueue();
+        if (fd < 0) return error.ReactorInitFailed;
+        var changes = [1]Kevent{.{
+            .ident = control_ident,
+            .filter = evfilt_user,
+            .flags = ev_add | ev_clear,
+            .fflags = 0,
+            .data = 0,
+            .udata = null,
+        }};
+        if (c.kevent(fd, changes[0..].ptr, 1, null, 0, null) < 0) {
+            _ = c.close(fd);
+            return error.ReactorInitFailed;
+        }
+        return .{ .fd = fd };
+    }
+
+    pub fn deinit(self: *Reactor) void {
+        if (self.fd >= 0) _ = c.close(self.fd);
+        self.fd = -1;
+    }
+
+    pub fn register(self: *Reactor, transport: Pty, token: *anyopaque) bool {
+        var changes = [2]Kevent{
+            .{ .ident = @intCast(transport.parent), .filter = evfilt_read, .flags = ev_add | ev_enable, .fflags = 0, .data = 0, .udata = token },
+            .{ .ident = @intCast(transport.parent), .filter = evfilt_write, .flags = ev_add | ev_disable, .fflags = 0, .data = 0, .udata = token },
+        };
+        return c.kevent(self.fd, changes[0..].ptr, @intCast(changes.len), null, 0, null) == 0;
+    }
+
+    pub fn remove(self: *Reactor, transport: Pty) void {
+        var changes = [2]Kevent{
+            .{ .ident = @intCast(transport.parent), .filter = evfilt_read, .flags = ev_delete, .fflags = 0, .data = 0, .udata = null },
+            .{ .ident = @intCast(transport.parent), .filter = evfilt_write, .flags = ev_delete, .fflags = 0, .data = 0, .udata = null },
+        };
+        _ = c.kevent(self.fd, changes[0..].ptr, @intCast(changes.len), null, 0, null);
+    }
+
+    pub fn setReadable(self: *Reactor, transport: Pty, enabled: bool, token: *anyopaque) void {
+        var changes = [1]Kevent{.{ .ident = @intCast(transport.parent), .filter = evfilt_read, .flags = if (enabled) ev_enable else ev_disable, .fflags = 0, .data = 0, .udata = token }};
+        _ = c.kevent(self.fd, changes[0..].ptr, 1, null, 0, null);
+    }
+
+    pub fn setWritable(self: *Reactor, transport: Pty, enabled: bool, token: *anyopaque) void {
+        var changes = [1]Kevent{.{ .ident = @intCast(transport.parent), .filter = evfilt_write, .flags = if (enabled) ev_enable else ev_disable, .fflags = 0, .data = 0, .udata = token }};
+        _ = c.kevent(self.fd, changes[0..].ptr, 1, null, 0, null);
+    }
+
+    pub fn wake(self: *Reactor) void {
+        var changes = [1]Kevent{.{ .ident = control_ident, .filter = evfilt_user, .flags = 0, .fflags = note_trigger, .data = 0, .udata = null }};
+        _ = c.kevent(self.fd, changes[0..].ptr, 1, null, 0, null);
+    }
+
+    pub fn wait(self: *Reactor, out: []ReactorEvent) usize {
+        var events: [64]Kevent = undefined;
+        const capacity = @min(events.len, out.len);
+        const count = c.kevent(self.fd, null, 0, events[0..].ptr, @intCast(capacity), null);
+        if (count <= 0) return 0;
+        var index: usize = 0;
+        while (index < @as(usize, @intCast(count))) : (index += 1) {
+            const event = events[index];
+            const control = event.filter == evfilt_user and event.ident == control_ident;
+            out[index] = .{
+                .token = event.udata,
+                .readable = event.filter == evfilt_read,
+                .writable = event.filter == evfilt_write,
+                .hangup = event.flags & (ev_eof | ev_error) != 0,
+                .control = control,
+            };
+        }
+        return @intCast(count);
+    }
+} else struct {
+    pub fn init() error{ReactorInitFailed}!Reactor {
+        return error.ReactorInitFailed;
+    }
+};
+
+const Kevent = extern struct {
+    ident: usize,
+    filter: i16,
+    flags: u16,
+    fflags: u32,
+    data: isize,
+    udata: ?*anyopaque,
+};
+
+const evfilt_read: i16 = -1;
+const evfilt_write: i16 = -2;
+const evfilt_user: i16 = -10;
+const ev_add: u16 = 0x0001;
+const ev_delete: u16 = 0x0002;
+const ev_enable: u16 = 0x0004;
+const ev_disable: u16 = 0x0008;
+const ev_eof: u16 = 0x8000;
+const ev_error: u16 = 0x4000;
+const ev_clear: u16 = 0x0020;
+const note_trigger: u32 = 0x01000000;
+
 /// Block until the transport is readable (`want_read`), writable
 /// (`want_write`), hung up, or the nudge pipe fires. A caller wanting
 /// NEITHER direction (a parked reader with nothing to write) waits on
diff --git a/node_modules/@native-sdk/cli/src/runtime/root.zig b/node_modules/@native-sdk/cli/src/runtime/root.zig
index d6a79e9..3a37b6d 100644
--- a/node_modules/@native-sdk/cli/src/runtime/root.zig
+++ b/node_modules/@native-sdk/cli/src/runtime/root.zig
@@ -160,6 +160,10 @@ pub const max_effect_ptys = runtime_effects.max_effect_ptys;
 pub const max_effect_pty_chunk_bytes = runtime_effects.max_effect_pty_chunk_bytes;
 pub const max_effect_pty_write_bytes = runtime_effects.max_effect_pty_write_bytes;
 pub const max_effect_pty_term_bytes = runtime_effects.max_effect_pty_term_bytes;
+pub const PtyEnvEntry = runtime_effects.PtyEnvEntry;
+pub const PtyEnvVar = runtime_effects.PtyEnvVar;
+pub const max_effect_pty_env_entries = runtime_effects.max_effect_pty_env_entries;
+pub const max_effect_pty_env_bytes = runtime_effects.max_effect_pty_env_bytes;
 pub const EffectHostResult = runtime_effects.EffectHostResult;
 pub const HostCallBinding = runtime_effects.HostCallBinding;
 pub const HostCallCompletion = runtime_effects.HostCallCompletion;
diff --git a/node_modules/@native-sdk/cli/src/runtime/terminal_session.zig b/node_modules/@native-sdk/cli/src/runtime/terminal_session.zig
index 808265e..0df466e 100644
--- a/node_modules/@native-sdk/cli/src/runtime/terminal_session.zig
+++ b/node_modules/@native-sdk/cli/src/runtime/terminal_session.zig
@@ -37,12 +37,21 @@ const std = @import("std");
 const builtin = @import("builtin");
 const canvas = @import("canvas");
 const effects = @import("effects.zig");
+const runtime_clock = @import("clock.zig");
 const term_vt = @import("terminal_vt");

 pub const enabled = term_vt.enabled;

-/// One session per pty slot: the effects engine's own table bound.
-pub const max_sessions: usize = effects.max_effect_ptys;
+const DeviceAttributesCallback = if (enabled)
+    @typeInfo(@FieldType(term_vt.vt.TerminalStream.Handler.Effects, "device_attributes")).optional.child
+else
+    void;
+const DeviceAttributesFunction = if (enabled) @typeInfo(DeviceAttributesCallback).pointer.child else void;
+const DeviceAttributes = if (enabled) @typeInfo(DeviceAttributesFunction).@"fn".return_type.? else void;
+
+/// Emulators are allocated on demand; rendering capacity is independent from
+/// the number of live PTY transports.
+pub const max_sessions: ?usize = null;

 /// The journaled pty verbs the store drives, type-erased so this module
 /// never names the Msg-generic engine: the app loop wraps its engine.
@@ -101,6 +110,14 @@ const DisabledStore = struct {
         _ = self;
         _ = event;
     }
+    pub fn notePtySpawn(self: *DisabledStore, pty: u64) void {
+        _ = self;
+        _ = pty;
+    }
+    pub fn forget(self: *DisabledStore, pty: u64) void {
+        _ = self;
+        _ = pty;
+    }
     pub fn hasSession(self: *const DisabledStore, pty: u64) bool {
         _ = self;
         _ = pty;
@@ -167,7 +184,8 @@ const EnabledStore = struct {
     /// for a stable address; built on first session create so apps with
     /// no `<terminal>` pay nothing.
     threaded: ?*std.Io.Threaded = null,
-    entries: [max_sessions]Entry = @splat(.{}),
+    entries: std.ArrayListUnmanaged(Entry) = .empty,
+    visible_keys: std.ArrayListUnmanaged(u64) = .empty,
     gateway: ?PtyGateway = null,
     /// The tokens the CURRENT build resolves snapshots with, stamped by
     /// `beginBuild` (the palette's theme mapping needs them). Valid for
@@ -184,10 +202,14 @@ const EnabledStore = struct {
     }

     pub fn deinit(self: *EnabledStore) void {
-        for (&self.entries) |*entry| {
+        for (self.entries.items) |*entry| {
             if (entry.session) |session| session.destroy();
             entry.* = .{};
         }
+        self.entries.deinit(self.allocator);
+        self.entries = .empty;
+        self.visible_keys.deinit(self.allocator);
+        self.visible_keys = .empty;
         if (self.threaded) |threaded| {
             threaded.deinit();
             self.allocator.destroy(threaded);
@@ -201,6 +223,7 @@ const EnabledStore = struct {

     pub fn beginBuild(self: *EnabledStore, tokens: canvas.DesignTokens) void {
         self.tokens = tokens;
+        self.visible_keys.clearRetainingCapacity();
     }

     /// The builder seam (`Ui.terminal_lookup`): finalize resolves each
@@ -214,7 +237,15 @@ const EnabledStore = struct {
     fn resolveOpaque(context: *anyopaque, pty: u64) ?*const canvas.TerminalGrid {
         const self: *EnabledStore = @ptrCast(@alignCast(context));
         const session = self.ensureSession(pty) orelse return null;
-        if (session.snapshot_dirty) {
+        var visible = false;
+        for (self.visible_keys.items) |key| {
+            if (key == pty) {
+                visible = true;
+                break;
+            }
+        }
+        if (!visible) self.visible_keys.append(self.allocator, pty) catch {};
+        if (session.snapshot_dirty and session.canPublishSnapshot()) {
             session.rebuildSnapshot(self.tokens) catch return &session.grid;
         }
         return &session.grid;
@@ -231,7 +262,7 @@ const EnabledStore = struct {

     fn find(self: *EnabledStore, pty: u64) ?*Session {
         if (pty == 0) return null;
-        for (&self.entries) |*entry| {
+        for (self.entries.items) |*entry| {
             if (entry.key == pty) return entry.session;
         }
         return null;
@@ -240,24 +271,43 @@ const EnabledStore = struct {
     fn ensureSession(self: *EnabledStore, pty: u64) ?*Session {
         if (pty == 0) return null;
         if (self.find(pty)) |session| return session;
-        for (&self.entries) |*entry| {
-            if (entry.session != null) continue;
-            const session_io = self.io() orelse return null;
-            const session = Session.create(self.allocator, session_io, 80, 24) catch return null;
-            entry.* = .{ .key = pty, .session = session };
-            return session;
-        }
-        return null;
+        const session_io = self.io() orelse return null;
+        const session = Session.create(self.allocator, session_io, 80, 24) catch return null;
+        self.entries.append(self.allocator, .{ .key = pty, .session = session }) catch {
+            session.destroy();
+            return null;
+        };
+        return session;
     }

     pub fn hasSession(self: *const EnabledStore, pty: u64) bool {
         if (pty == 0) return false;
-        for (&self.entries) |*entry| {
+        for (self.entries.items) |*entry| {
             if (entry.key == pty and entry.session != null) return true;
         }
         return false;
     }

+    /// Reset an emulator at the accepted-spawn boundary rather than waiting
+    /// for the child's first output. Keys may be recycled after exit delivery;
+    /// the new tab must start blank even when the child is initially silent.
+    pub fn notePtySpawn(self: *EnabledStore, pty: u64) void {
+        self.forget(pty);
+    }
+
+    /// Release an emulator and all of its retained scrollback/render buffers.
+    /// Effects owns the transport separately and calls this only after the app
+    /// has observed its terminal exit and removes the bound terminal widget.
+    pub fn forget(self: *EnabledStore, pty: u64) void {
+        if (pty == 0) return;
+        for (self.entries.items, 0..) |entry, index| {
+            if (entry.key != pty) continue;
+            if (entry.session) |session| session.destroy();
+            _ = self.entries.orderedRemove(index);
+            return;
+        }
+    }
+
     /// The effects engine's pty tap: every delivered pty event — live
     /// drain and replay feed alike — passes through here at the same
     /// instant the app's Msg is built, so the emulator's inputs are
@@ -449,9 +499,18 @@ const EnabledStore = struct {
     /// caller's re-emit trigger.
     pub fn refreshDirty(self: *EnabledStore) bool {
         var any = false;
-        for (&self.entries) |*entry| {
+        for (self.entries.items) |*entry| {
             const session = entry.session orelse continue;
             if (!session.snapshot_dirty) continue;
+            var visible = false;
+            for (self.visible_keys.items) |key| {
+                if (key == entry.key) {
+                    visible = true;
+                    break;
+                }
+            }
+            if (!visible) continue;
+            if (!session.canPublishSnapshot()) continue;
             any = true;
             session.rebuildSnapshot(self.tokens) catch {};
         }
@@ -465,7 +524,7 @@ const EnabledStore = struct {
     /// whether any bytes moved.
     pub fn flushPending(self: *EnabledStore) bool {
         var moved = false;
-        for (&self.entries) |*entry| {
+        for (self.entries.items) |*entry| {
             const session = entry.session orelse continue;
             if (session.ended) continue;
             if (session.outbound_len == 0 and session.response_len == 0) continue;
@@ -537,6 +596,8 @@ const Session = if (enabled) struct {
     /// Session lifecycle: exactly one exit ends every spawn; output on
     /// an ended session is a new spawn reusing the key.
     ended: bool = false,
+    clock: runtime_clock.Clock = .system,
+    synchronized_since_ms: ?u64 = null,

     /// View state for the source-wins scrollback echo and the
     /// `on-terminal` change compare.
@@ -602,7 +663,7 @@ const Session = if (enabled) struct {
             .bell = null,
             .clipboard_write = null,
             .color_scheme = null,
-            .device_attributes = null,
+            .device_attributes = deviceAttributes,
             .enquiry = null,
             .size = null,
             .title_changed = null,
@@ -656,6 +717,7 @@ const Session = if (enabled) struct {
         session.wheel_accum = 0;
         session.macos_natural_keys_held = 0;
         session.ended = false;
+        session.synchronized_since_ms = null;
         session.grid.running = true;
         session.snapshot_dirty = true;
     }
@@ -676,6 +738,11 @@ const Session = if (enabled) struct {
             offset = end;
         }
         if (bytes.len == 0) session.moveResponsesToOutbound(gateway, key);
+        if (session.term.modes.get(.synchronized_output)) {
+            if (session.synchronized_since_ms == null) session.synchronized_since_ms = session.clock.monotonicMs();
+        } else {
+            session.synchronized_since_ms = null;
+        }
         // The child produced output, so it is reading: push pending
         // outbound the freed FIFO will now take.
         session.flushOutbound(gateway, key);
@@ -703,6 +770,13 @@ const Session = if (enabled) struct {
         session.response_len += bytes.len;
     }

+    /// Advertise the libghostty-vt defaults (VT220 + ANSI color). Leaving
+    /// this callback null makes DA1 queries disappear even though write_pty
+    /// is wired, which stalls shells such as fish during startup.
+    fn deviceAttributes(_: *vt.TerminalStream.Handler) DeviceAttributes {
+        return .{};
+    }
+
     fn clearResponses(session: *Session) void {
         session.response_len = 0;
     }
@@ -1238,14 +1312,31 @@ const Session = if (enabled) struct {

     // --------------------------------------------------------- snapshot

+    /// DEC synchronized output brackets one logical TUI frame. Keep the last
+    /// published snapshot until the closing marker, with Ghostty's one-second
+    /// safety timeout for a producer that never closes its transaction.
+    fn canPublishSnapshot(session: *Session) bool {
+        if (session.ended or !session.term.modes.get(.synchronized_output)) {
+            session.synchronized_since_ms = null;
+            return true;
+        }
+        const now = session.clock.monotonicMs();
+        const started = session.synchronized_since_ms orelse now;
+        session.synchronized_since_ms = started;
+        if (now -| started < 1000) return false;
+        session.term.modes.set(.synchronized_output, false);
+        session.synchronized_since_ms = null;
+        return true;
+    }
+
     /// Rebuild the published `canvas.TerminalGrid` from the emulator's
     /// render state, colors RESOLVED per the painter's contract: the
-    /// theme-derived ANSI-16 story (untouched palette entries derive
-    /// from the active tokens; an OSC-restyled slot wins verbatim),
-    /// exact 256-color and truecolor pass-through, inverse/faint/
+    /// emulator-owned ANSI/256-color palette (including OSC overrides),
+    /// exact truecolor pass-through, inverse/faint/
     /// invisible folded in, and a spacer cell's background pre-resolved
     /// to extend its wide primary's.
     fn rebuildSnapshot(session: *Session, tokens: canvas.DesignTokens) !void {
+        if (!session.canPublishSnapshot()) return;
         const gpa = session.gpa;
         // Push the theme colors into the emulator's DEFAULTS (never the
         // OSC overrides) so ghostty itself composes the final
@@ -1260,7 +1351,7 @@ const Session = if (enabled) struct {

         try session.render.update(gpa, &session.term);
         const rs = &session.render;
-        const palette = Palette.init(tokens, &rs.colors, &session.term.colors.palette);
+        const palette = Palette.init(&rs.colors, &session.term.colors.palette);

         const row_count = @min(rs.row_data.len, canvas.max_terminal_rows);
         var col_count: usize = 0;
@@ -1464,58 +1555,24 @@ fn rgbToColor(rgb: anytype) canvas.Color {
     return canvas.Color.rgb8(rgb.r, rgb.g, rgb.b);
 }

-/// The theme mapping, stated plainly: where the emulator's palette entry
-/// still holds its DEFAULT value, the ANSI-16 slot derives from the
-/// active theme tokens so a themed app and its terminal read as one
-/// surface. The moment a program restyles an entry (OSC 4), the
-/// programmed color wins verbatim — and the cube (16..231), the
-/// grayscale ramp (232..255), and truecolor always pass through exactly.
-/// (The example tier's palette, promoted with the session store.)
+/// Preserve the emulator's complete palette verbatim. libghostty-vt owns the
+/// readable ANSI-16 defaults, xterm cube/grayscale entries, and OSC 4 runtime
+/// overrides; the renderer must not replace or dim the first 16 slots. RGB
+/// styles bypass this table below and therefore remain exact truecolor.
 const Palette = if (enabled) struct {
     background: canvas.Color,
     foreground: canvas.Color,
-    ansi: [16]canvas.Color,
-    /// The emulator's live palette WITH its override mask, so an
-    /// explicit OSC 4 set is honored even when it equals the default
-    /// RGB — the mask, not RGB equality, decides "untouched".
     dynamic: *const vt.color.DynamicPalette,

-    fn init(tokens: canvas.DesignTokens, terminal_colors: *const vt.RenderState.Colors, dynamic: *const vt.color.DynamicPalette) Palette {
-        const colors = tokens.colors;
-        const dark = colors.background.r + colors.background.g + colors.background.b < 1.5;
-        const dim: f32 = if (dark) 0.85 else 1.0;
-        const bright: f32 = if (dark) 1.0 else 0.8;
+    fn init(terminal_colors: *const vt.RenderState.Colors, dynamic: *const vt.color.DynamicPalette) Palette {
         return .{
             .background = rgbToColor(terminal_colors.background),
             .foreground = rgbToColor(terminal_colors.foreground),
             .dynamic = dynamic,
-            .ansi = .{
-                // 0-7: black, red, green, yellow, blue, magenta, cyan, white.
-                blend(colors.text, colors.background, if (dark) 0.35 else 0.95),
-                scale(colors.destructive, dim),
-                scale(colors.success, dim),
-                scale(colors.warning, dim),
-                scale(canvas.Color.rgb8(37, 99, 235), dim),
-                scale(canvas.Color.rgb8(147, 51, 234), dim),
-                scale(canvas.Color.rgb8(8, 145, 178), dim),
-                blend(colors.text, colors.background, if (dark) 0.75 else 0.35),
-                // 8-15: the bright ramp.
-                blend(colors.text, colors.background, if (dark) 0.5 else 0.75),
-                scale(colors.destructive, bright),
-                scale(colors.success, bright),
-                scale(colors.warning, bright),
-                scale(canvas.Color.rgb8(59, 130, 246), bright),
-                scale(canvas.Color.rgb8(168, 85, 247), bright),
-                scale(canvas.Color.rgb8(34, 211, 238), bright),
-                colors.text,
-            },
         };
     }

     fn indexed(palette: *const Palette, index: u8) canvas.Color {
-        if (index < 16 and !palette.dynamic.mask.isSet(index)) {
-            return palette.ansi[index];
-        }
         const live = palette.dynamic.current[index];
         return canvas.Color.rgb8(live.r, live.g, live.b);
     }
@@ -1557,13 +1614,4 @@ const Palette = if (enabled) struct {
             1,
         );
     }
-
-    fn scale(color: canvas.Color, factor: f32) canvas.Color {
-        return canvas.Color.rgba(
-            @min(1, color.r * factor),
-            @min(1, color.g * factor),
-            @min(1, color.b * factor),
-            1,
-        );
-    }
 } else struct {};
diff --git a/node_modules/@native-sdk/cli/src/runtime/terminal_session_tests.zig b/node_modules/@native-sdk/cli/src/runtime/terminal_session_tests.zig
index 951dbaa..2cb876c 100644
--- a/node_modules/@native-sdk/cli/src/runtime/terminal_session_tests.zig
+++ b/node_modules/@native-sdk/cli/src/runtime/terminal_session_tests.zig
@@ -87,12 +87,90 @@ test "binding a pty key publishes a live grid that carries fed output as real ce
     try testing.expect(refreshed.rows.len > 0);
     try testing.expectEqual(@as(u21, 'h'), refreshed.rows[0].cells[0].cp);
     try testing.expectEqualStrings("h", refreshed.rows[0].cells[0].cluster);
-    // A default (untouched) ANSI-31 red resolves through the theme's
-    // destructive token, the palette-honesty story.
-    const tokens: canvas.DesignTokens = .{};
+    // ANSI-31 is libghostty-vt's exact default red (#cc6666), not a
+    // theme-token replacement or a dimmed approximation.
     const red_cell = refreshed.rows[0].cells[6];
     try testing.expectEqual(@as(u21, 'w'), red_cell.cp);
-    try testing.expectApproxEqAbs(tokens.colors.destructive.r, red_cell.fg.r, 0.2);
+    try testing.expectApproxEqAbs(@as(f32, 0xcc) / 255.0, red_cell.fg.r, 0.001);
+    try testing.expectApproxEqAbs(@as(f32, 0x66) / 255.0, red_cell.fg.g, 0.001);
+    try testing.expectApproxEqAbs(@as(f32, 0x66) / 255.0, red_cell.fg.b, 0.001);
+
+    feedOutput(&store, 7, "\x1b[38;2;1;2;3mX\x1b[0m");
+    const truecolor = resolveGrid(&store, 7) orelse return error.TestExpectedGrid;
+    const rgb_cell = truecolor.rows[1].cells[2];
+    try testing.expectEqual(@as(u21, 'X'), rgb_cell.cp);
+    try testing.expectApproxEqAbs(@as(f32, 1) / 255.0, rgb_cell.fg.r, 0.001);
+    try testing.expectApproxEqAbs(@as(f32, 2) / 255.0, rgb_cell.fg.g, 0.001);
+    try testing.expectApproxEqAbs(@as(f32, 3) / 255.0, rgb_cell.fg.b, 0.001);
+}
+
+test "failed session creation never retains an empty store entry" {
+    if (comptime !terminal_session.enabled) return error.SkipZigTest;
+    for (0..24) |fail_index| {
+        var failing = std.testing.FailingAllocator.init(testing.allocator, .{ .fail_index = fail_index });
+        var store = TerminalSessions.init(failing.allocator());
+        store.beginBuild(.{});
+        _ = resolveGrid(&store, 77);
+        for (store.entries.items) |entry| try testing.expect(entry.session != null);
+        store.deinit();
+    }
+}
+
+test "alternate screen switches across PTY chunks and restores the primary screen" {
+    if (comptime !terminal_session.enabled) return error.SkipZigTest;
+    var store = TerminalSessions.init(testing.allocator);
+    defer store.deinit();
+    store.beginBuild(.{});
+    _ = store.reconcile(91, 0, 20, 6);
+    feedOutput(&store, 91, "PRIMARY-MARKER");
+    _ = resolveGrid(&store, 91) orelse return error.TestExpectedGrid;
+
+    // PTY read boundaries may split a control sequence at any byte.
+    feedOutput(&store, 91, "\x1b[?10");
+    feedOutput(&store, 91, "49h\x1b[2J\x1b[HALTERNATE-MARKER");
+    const alternate = resolveGrid(&store, 91) orelse return error.TestExpectedGrid;
+    try testing.expect(std.mem.indexOf(u8, gridText(alternate), "ALTERNATE-MARKER") != null);
+    try testing.expect(std.mem.indexOf(u8, gridText(alternate), "PRIMARY-MARKER") == null);
+    try testing.expectEqual(@as(u21, 'A'), alternate.rows[0].cells[0].cp);
+
+    feedOutput(&store, 91, "\x1b[?1049l");
+    const primary = resolveGrid(&store, 91) orelse return error.TestExpectedGrid;
+    try testing.expect(std.mem.indexOf(u8, gridText(primary), "PRIMARY-MARKER") != null);
+    try testing.expect(std.mem.indexOf(u8, gridText(primary), "ALTERNATE-MARKER") == null);
+    try testing.expectEqual(@as(u21, 'P'), primary.rows[0].cells[0].cp);
+}
+
+test "synchronized TUI output publishes only the completed frame" {
+    if (comptime !terminal_session.enabled) return error.SkipZigTest;
+    var store = TerminalSessions.init(testing.allocator);
+    defer store.deinit();
+    store.beginBuild(.{});
+    _ = store.reconcile(92, 0, 20, 6);
+    feedOutput(&store, 92, "COMPLETE-FRAME");
+    _ = resolveGrid(&store, 92) orelse return error.TestExpectedGrid;
+    feedOutput(&store, 92, "\x1b[?2026h\x1b[2J\x1b[HPARTIAL");
+    const held = resolveGrid(&store, 92) orelse return error.TestExpectedGrid;
+    try testing.expect(std.mem.indexOf(u8, gridText(held), "COMPLETE-FRAME") != null);
+    feedOutput(&store, 92, "-FRAME\x1b[?2026l");
+    const complete = resolveGrid(&store, 92) orelse return error.TestExpectedGrid;
+    try testing.expect(std.mem.indexOf(u8, gridText(complete), "PARTIAL-FRAME") != null);
+    try testing.expect(std.mem.indexOf(u8, gridText(complete), "COMPLETE-FRAME") == null);
+}
+
+test "an unfinished synchronized frame is published after the safety timeout" {
+    if (comptime !terminal_session.enabled) return error.SkipZigTest;
+    var store = TerminalSessions.init(testing.allocator);
+    defer store.deinit();
+    store.beginBuild(.{});
+    _ = resolveGrid(&store, 93) orelse return error.TestExpectedGrid;
+    var clock: @import("clock.zig").TestClock = .{};
+    store.entries.items[0].session.?.clock = clock.clock();
+    feedOutput(&store, 93, "\x1b[?2026hTIMEOUT-FRAME");
+    try testing.expect(!store.refreshDirty());
+    clock.advanceMs(1000);
+    try testing.expect(store.refreshDirty());
+    const grid = resolveGrid(&store, 93) orelse return error.TestExpectedGrid;
+    try testing.expect(std.mem.indexOf(u8, gridText(grid), "TIMEOUT-FRAME") != null);
 }

 test "pointer gestures select terminal cells words and lines for clipboard copy" {
@@ -386,7 +464,7 @@ test "typing while scrolled back snaps the viewport to the live screen" {
     try testing.expectEqual(@as(u32, 0), (store.currentState(5) orelse return error.TestExpectedState).scrollback);
 }

-test "an exit parks the session and output on the reused key resets to a clean terminal" {
+test "an accepted respawn clears a reused key before the first output" {
     if (comptime !terminal_session.enabled) return error.SkipZigTest;
     var store = TerminalSessions.init(testing.allocator);
     defer store.deinit();
@@ -404,8 +482,14 @@ test "an exit parks the session and output on the reused key resets to a clean t
     try testing.expect(!store.textInput(2, "late"));
     try testing.expect(!store.keyEvent(2, .{ .phase = .key_down, .key = "enter" }));

-    // A new spawn reused the key: its first output resets the emulator
-    // before feeding, so the old screen and modes never leak in.
+    // A new spawn reused the key: accepting it resets the emulator before the
+    // child emits anything, so the new tab cannot flash the old scrollback.
+    store.notePtySpawn(2);
+    try testing.expect(!store.hasSession(2));
+    const blank = resolveGrid(&store, 2) orelse return error.TestExpectedGrid;
+    try testing.expect(blank.running);
+    try testing.expect(std.mem.indexOf(u8, gridText(blank), "old session") == null);
+
     feedOutput(&store, 2, "fresh prompt $ ");
     const fresh = resolveGrid(&store, 2) orelse return error.TestExpectedGrid;
     try testing.expect(fresh.running);
@@ -428,6 +512,12 @@ test "emulator query answers write back to the pty through the ring" {
     feedOutput(&store, 4, "\x1b[6n");
     try testing.expect(std.mem.indexOf(u8, gw.written.items, "\x1b[") != null);
     try testing.expect(std.mem.indexOf(u8, gw.written.items, "R") != null);
+
+    // Primary Device Attributes must answer as well. Fish probes this during
+    // startup and otherwise waits two seconds before disabling capabilities.
+    gw.written.clearRetainingCapacity();
+    feedOutput(&store, 4, "\x1b[c");
+    try testing.expectEqualStrings("\x1b[?62;22c", gw.written.items);
 }

 test "the wide-cell spacer extends its primary's background in the published snapshot" {
diff --git a/node_modules/@native-sdk/cli/src/runtime/ts_core_host.zig b/node_modules/@native-sdk/cli/src/runtime/ts_core_host.zig
index becd14f..063c209 100644
--- a/node_modules/@native-sdk/cli/src/runtime/ts_core_host.zig
+++ b/node_modules/@native-sdk/cli/src/runtime/ts_core_host.zig
@@ -718,7 +718,7 @@ pub fn TsCoreHost(comptime core: type) type {
         var images: [runtime_effects.max_effects]ImageEntry = @splat(.{});
         var channels: [runtime_effects.max_effect_channels]ChannelEntry = @splat(.{});
         var audio_captures: [runtime_effects.max_effect_channels]AudioCaptureEntry = @splat(.{});
-        var ptys: [runtime_effects.max_effect_ptys]PtyEntry = @splat(.{});
+        var ptys: std.ArrayListUnmanaged(PtyEntry) = .empty;
         var dbs: [runtime_effects.max_db_effects]DbEntry = @splat(.{});
         /// The platform caches directory for URL image sources, the
         /// audio cache dir's twin (`setImageCacheDir` / `TsUiApp`'s
@@ -800,7 +800,7 @@ pub fn TsCoreHost(comptime core: type) type {
             images = @splat(.{});
             channels = @splat(.{});
             audio_captures = @splat(.{});
-            ptys = @splat(.{});
+            for (ptys.items) |*entry| entry.* = .{};
             dbs = @splat(.{});
             clip_write_counter = 0;
             audio_cache_dir_len = 0;
@@ -2647,7 +2647,7 @@ pub fn TsCoreHost(comptime core: type) type {
                 fx.stageLoopMsg(msgFromTagPty(event_tag, fx.stageLoopKey(key), true, .{ .key = 0, .kind = .exit, .reason = .rejected }));
                 return;
             };
-            const entry = &ptys[index];
+            const entry = &ptys.items[index];
             entry.used = true;
             entry.key_len = key.len;
             @memcpy(entry.key[0..key.len], key);
@@ -2700,17 +2700,18 @@ pub fn TsCoreHost(comptime core: type) type {

         fn findPty(key: []const u8) ?usize {
             if (key.len == 0) return null;
-            for (&ptys, 0..) |*entry, index| {
+            for (ptys.items, 0..) |*entry, index| {
                 if (entry.used and std.mem.eql(u8, entry.wireKey(), key)) return index;
             }
             return null;
         }

         fn freePtyIndex() ?usize {
-            for (&ptys, 0..) |*entry, index| {
+            for (ptys.items, 0..) |*entry, index| {
                 if (!entry.used) return index;
             }
-            return null;
+            ptys.append(std.heap.page_allocator, .{}) catch return null;
+            return ptys.items.len - 1;
         }

         /// `PtyMsgFn` for pty sessions: every event routes the entry's
@@ -2722,10 +2723,10 @@ pub fn TsCoreHost(comptime core: type) type {
                 @panic("ts core host: a pty event arrived outside the bridge's pty key namespace");
             }
             const index = event.key - pty_key_base;
-            if (index >= ptys.len or !ptys[index].used) {
+            if (index >= ptys.items.len or !ptys.items[index].used) {
                 @panic("ts core host: a pty event arrived for a session the bridge is not tracking");
             }
-            const entry = &ptys[index];
+            const entry = &ptys.items[index];
             const wire_key = entry.wireKey();
             if (event.kind == .exit) entry.used = false;
             // Live delivery is same-frame, so the key rides the frame arena.
diff --git a/node_modules/@native-sdk/cli/src/runtime/ui_app.zig b/node_modules/@native-sdk/cli/src/runtime/ui_app.zig
index 02589cb..3534ef6 100644
--- a/node_modules/@native-sdk/cli/src/runtime/ui_app.zig
+++ b/node_modules/@native-sdk/cli/src/runtime/ui_app.zig
@@ -1321,8 +1321,7 @@ pub fn UiAppWithFeatures(comptime ModelT: type, comptime MsgT: type, comptime fe
         /// the nested rebuild each dispatch runs reconciles to the same
         /// state and produces nothing new (`terminal_state_draining`
         /// makes that one level a hard guarantee).
-        terminal_state_pending: [terminal_session.max_sessions]PendingTerminalState = @splat(.{}),
-        terminal_state_pending_count: usize = 0,
+        terminal_state_pending: std.ArrayListUnmanaged(PendingTerminalState) = .empty,
         terminal_state_draining: bool = false,
         /// Live model-declared secondary windows (`Options.windows_fn`),
         /// keyed by window label.
@@ -1447,6 +1446,7 @@ pub fn UiAppWithFeatures(comptime ModelT: type, comptime MsgT: type, comptime fe
             // memory only and never touches the dead platform.
             self.effects.deinit();
             self.terminal_sessions.deinit();
+            self.terminal_state_pending.deinit(self.backing);
             self.arenas[0].deinit();
             self.arenas[1].deinit();
             self.markup_arenas[0].deinit();
@@ -1554,7 +1554,12 @@ pub fn UiAppWithFeatures(comptime ModelT: type, comptime MsgT: type, comptime fe
             // emulator query answers) and grid sizes back through the
             // JOURNALED pty verbs. Idempotent, like every bind here.
             if (comptime terminal_session.enabled) {
-                self.effects.pty_tap = .{ .context = @ptrCast(self), .notify = terminalPtyTap };
+                self.effects.pty_tap = .{
+                    .context = @ptrCast(self),
+                    .notify = terminalPtyTap,
+                    .spawn = terminalPtySpawn,
+                    .forget = terminalPtyForget,
+                };
                 self.terminal_sessions.setGateway(.{
                     .context = @ptrCast(self),
                     .write = terminalGatewayWrite,
@@ -1568,6 +1573,16 @@ pub fn UiAppWithFeatures(comptime ModelT: type, comptime MsgT: type, comptime fe
             self.terminal_sessions.notePtyEvent(event);
         }

+        fn terminalPtySpawn(context: *anyopaque, key: u64) void {
+            const self: *Self = @ptrCast(@alignCast(context));
+            self.terminal_sessions.notePtySpawn(key);
+        }
+
+        fn terminalPtyForget(context: *anyopaque, key: u64) void {
+            const self: *Self = @ptrCast(@alignCast(context));
+            self.terminal_sessions.forget(key);
+        }
+
         fn terminalGatewayWrite(context: *anyopaque, key: u64, bytes: []const u8) bool {
             const self: *Self = @ptrCast(@alignCast(context));
             return self.effects.ptyWrite(key, bytes);
@@ -1779,6 +1794,11 @@ pub fn UiAppWithFeatures(comptime ModelT: type, comptime MsgT: type, comptime fe
                     if (rebuild_error == null) rebuild_error = err;
                 };
             }
+            if (self.effects.hasPending()) {
+                if (runtime.findViewIndex(self.canvas_window_id, self.options.canvas_label)) |view_index| {
+                    runtime.requestCanvasFrameForView(view_index) catch {};
+                }
+            }
             if (rebuild_error) |err| return err;
             // A Msg dispatched FROM a secondary window still rebuilt the
             // main canvas above (one model, every window's view derives
@@ -1822,7 +1842,13 @@ pub fn UiAppWithFeatures(comptime ModelT: type, comptime MsgT: type, comptime fe
             // an earlier dispatch, so replay's file-order feed always
             // finds the parked request (see Effects.DrainBoundary).
             var boundary = self.effects.drainBoundary();
-            while (self.effects.takeMsgWithin(&boundary)) |msg| {
+            // Bound one UI transaction even when many PTYs are producing.
+            // Remaining work is paced by a requested GPU frame below, keeping
+            // pointer/keyboard/window events responsive under terminal floods.
+            const max_effect_messages_per_frame: usize = 64;
+            var delivered: usize = 0;
+            while (delivered < max_effect_messages_per_frame) : (delivered += 1) {
+                const msg = self.effects.takeMsgWithin(&boundary) orelse break;
                 self.applyMsg(msg);
                 dispatched = true;
             }
@@ -2231,13 +2257,10 @@ pub fn UiAppWithFeatures(comptime ModelT: type, comptime MsgT: type, comptime fe
                     proposed.y,
                 ) orelse continue;
                 applied = true;
-                if (self.terminal_state_pending_count < self.terminal_state_pending.len) {
-                    self.terminal_state_pending[self.terminal_state_pending_count] = .{
-                        .id = node.widget.id,
-                        .state = state,
-                    };
-                    self.terminal_state_pending_count += 1;
-                }
+                self.terminal_state_pending.append(self.backing, .{
+                    .id = node.widget.id,
+                    .state = state,
+                }) catch {};
             }
             // An applied change moved the published snapshot AFTER this
             // rebuild handed its layout over: refresh the retained
@@ -2270,13 +2293,10 @@ pub fn UiAppWithFeatures(comptime ModelT: type, comptime MsgT: type, comptime fe
         fn drainTerminalStates(self: *Self, runtime: *Runtime, window_id: platform.WindowId) anyerror!void {
             if (comptime !terminal_session.enabled) return;
             if (self.terminal_state_draining) return;
-            if (self.terminal_state_pending_count == 0) return;
+            if (self.terminal_state_pending.items.len == 0) return;
             self.terminal_state_draining = true;
             defer self.terminal_state_draining = false;
-            while (self.terminal_state_pending_count > 0) {
-                self.terminal_state_pending_count -= 1;
-                const pending = self.terminal_state_pending[self.terminal_state_pending_count];
-                self.terminal_state_pending[self.terminal_state_pending_count] = .{};
+            while (self.terminal_state_pending.pop()) |pending| {
                 const tree = self.tree orelse continue;
                 if (tree.msgForTerminal(pending.id, pending.state)) |msg| {
                     try self.dispatch(runtime, window_id, msg);
@@ -4638,6 +4658,9 @@ pub fn UiAppWithFeatures(comptime ModelT: type, comptime MsgT: type, comptime fe
             // identically. A no-op when nothing is pending.
             if (comptime terminal_session.enabled) {
                 _ = self.terminal_sessions.flushPending();
+                // Also service the synchronized-output timeout when a child
+                // stops producing bytes before its closing frame marker.
+                try self.repaintTerminals(runtime, frame_event.window_id);
             }
             try self.presentFrame(
                 runtime,
